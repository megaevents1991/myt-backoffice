Review the specified file(s) for React 19 best practices.

Audit each file against `@.claude/rules/standards/react.md` (single source of truth).
shadcn/ui (Radix) primitives only - don't reinvent `components/ui/`.

Output per file: each rule **PASS** or **FAIL [line X]** + a one-line fix.

$ARGUMENTS
