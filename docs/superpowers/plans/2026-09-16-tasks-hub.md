# Tasks Hub Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** להפוך את `/tasks` למרכז העבודה של הצוות — משימות שבועיות שהמערכת מייצרת לפי כללים ומשייכת ליוזרים, פתיל שיח עם תמונות ואזכורים, וכל ה-ROADMAP בפנים.

**Architecture:** הכלל הוא דאטה (`task_rules`), הגנרטור הוא קוד (`lib/services/task-rules/<domain>.ts`), קרון שבועי אחד עובר על הכללים הפעילים ומייצר סקירה שבועית או משימה-לפריט. השיח הוא טבלה אחת (`task_comments`) שמחזיקה גם תגובות וגם שורות activity, עם תמונות בבאקט פרטי. הרודמפ נכנס בסקריפט ייבוא חד-כיווני לשדות `board`/`phase`/`channel`/`progress`.

**Tech Stack:** Next.js 15 App Router · React 19 · TypeScript · Supabase (service-role, server-side) · shadcn/ui + Tailwind · Vercel cron · ZeptoMail דרך `lib/email.ts`

**Spec:** `docs/superpowers/specs/2026-09-16-tasks-hub-design.md`

**Worktree:** `.claude/worktrees/feat-tasks-hub`, ברנץ' `feat/tasks-hub` (נוצר מ-`08b014e`). מאסטר מוחזק ע"י סשן אחר — לא נוגעים בו.

## Global Constraints

- **אין test suite בריפו.** האימות הוא: `npx tsc --noEmit` (שער הטיפוסים האמיתי — הבילד מתעלם משגיאות), `npm run lint`, וסקריפטי self-test טהורים ב-`scripts/*-selftest.ts` שרצים ב-`npx tsx` בלי DB ובלי רשת — בדיוק הדפוס של `scripts/price-light-selftest.ts` ו-`scripts/agents-selftest.ts`. לוגיקה שדורשת DB נבדקת ב-`?dry_run=1`.
- **מיגרציה אחת בלבד**, `npm run db:new tasks_hub`, אחרי `git fetch origin && git merge origin/master`. **אסור** `npm run db:push` ואסור להריץ את ה-workflow מברנץ' — המיגרציה מוחלת רק כשה-PR נכנס למאסטר.
- **בלי CHECK constraints** על `status`/`priority`/`board`/`channel`/`domain`/`mode` — ולידציה בקוד בלבד.
- **מחיקות רכות** בכל מקום (`deleted_at`). אף פעם לא `delete` על `tasks`/`events`.
- **בלי ספריות UI חדשות.** shadcn/Radix + Tailwind + HTML5 drag נייטיב בלבד.
- **בלי `any`** חוץ מ-`const db = supabase as any` אחד לכל קובץ שניגש לטבלה שאין לה עדיין טיפוס מיוצר, עם `// eslint-disable-next-line @typescript-eslint/no-explicit-any` צמוד — הדפוס הקיים.
- **Server Actions**, לא API routes, לכל מוטציה של המסך. קרון ב-`app/api/cron/` עם `guardCronRoute(request)` בשורה הראשונה.
- **בלי קומיט אוטומטי.** כל טסק נגמר ב"עצור ודווח לדור"; דור סוקר ומריץ `/commit-push`. הודעת הקומיט המוצעת כתובה בכל טסק, בלי שורת co-author.
- הודעות משתמש בעברית, שמות קוד באנגלית.

---

### Task 1: מיגרציה + טיפוסים

**Files:**
- Create: `supabase/migrations/<timestamp>_tasks_hub.sql` (נוצר ב-`npm run db:new tasks_hub`)
- Create: `types/task-rule.types.ts`
- Create: `types/task-comment.types.ts`
- Modify: `types/task.types.ts`

**Interfaces:**
- Consumes: `TaskSourceRef`, `TaskPriority` הקיימים.
- Produces: `TASK_BOARDS`/`TaskBoard`, `MKT_CHANNELS`/`MktChannel`, `TaskStatus` עם `paused`, `TaskSource` עם `recurring`+`roadmap`, `Task` עם `board|phase|channel|progress`; `TaskRule`, `RuleDomain`, `RuleMode`, `RuleMatch`; `TaskComment`, `TaskCommentWithAuthor`, `TaskAttachment`, `TaskActivity`.

- [ ] **Step 1: לוודא מאסטר נקי ולייצר קובץ מיגרציה**

```bash
git fetch origin && git merge origin/master && npm run db:new tasks_hub
```

- [ ] **Step 2: לכתוב את ה-SQL**

```sql
-- Tasks Hub: recurring rule engine, task thread, roadmap fields.
-- Spec: docs/superpowers/specs/2026-09-16-tasks-hub-design.md
-- No CHECK constraints on purpose (repo rule): values are validated in
-- lib/actions/task-actions.ts and lib/actions/task-rule-actions.ts.

alter table public.tasks add column if not exists board    text not null default 'ops';
alter table public.tasks add column if not exists phase    smallint;
alter table public.tasks add column if not exists channel  text;
alter table public.tasks add column if not exists progress smallint;

-- 'paused' counts as OPEN, so the partial index must include it.
drop index if exists tasks_assignee_open_idx;
create index if not exists tasks_assignee_open_idx
  on public.tasks (assignee_id)
  where deleted_at is null and status in ('todo', 'in_progress', 'paused');

-- Since when is this event red? Needed by the price_light rule's min_weeks_red.
alter table public.events add column if not exists light_red_since timestamptz;

create table if not exists public.task_rules (
  id           uuid primary key default gen_random_uuid(),
  name         text not null,
  domain       text not null,
  mode         text not null default 'weekly_digest',
  match        jsonb not null default '{}'::jsonb,
  assignee_id  uuid references public.user_profiles(id) on delete set null,
  priority     text not null default 'medium',
  due_days     smallint,
  dow          smallint not null default 0,
  board        text not null default 'ops',
  title        text,
  description  text,
  active       boolean not null default true,
  last_run_at  timestamptz,
  created_by   uuid references public.user_profiles(id) on delete set null,
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists task_rules_active_idx on public.task_rules (domain) where active;
alter table public.task_rules enable row level security;

create table if not exists public.task_comments (
  id          uuid primary key default gen_random_uuid(),
  task_id     uuid not null references public.tasks(id) on delete cascade,
  author_id   uuid references public.user_profiles(id) on delete set null,
  kind        text not null default 'comment',
  body        text,
  activity    jsonb,
  attachments jsonb not null default '[]'::jsonb,
  mentions    uuid[] not null default '{}',
  edited_at   timestamptz,
  deleted_at  timestamptz,
  created_at  timestamptz not null default now()
);
create index if not exists task_comments_task_idx on public.task_comments (task_id, created_at);
alter table public.task_comments enable row level security;

-- Private: a task screenshot can carry a customer name or a supplier price.
insert into storage.buckets (id, name, public)
values ('task-attachments', 'task-attachments', false)
on conflict (id) do nothing;
```

- [ ] **Step 3: להרחיב את `types/task.types.ts`**

```ts
export const TASK_STATUSES = ["todo", "in_progress", "paused", "done", "cancelled"] as const;

export const TASK_SOURCES = [
  "manual", "creative_gap", "price_review", "price_light", "recurring", "roadmap",
] as const;

/** dev = roadmap product work · marketing = campaigns · ops = what the system generates. */
export const TASK_BOARDS = ["dev", "marketing", "ops"] as const;
export type TaskBoard = (typeof TASK_BOARDS)[number];

export const MKT_CHANNELS = [
  "social", "email", "seo", "ads", "content", "partnerships", "pr",
] as const;
export type MktChannel = (typeof MKT_CHANNELS)[number];
```

ובתוך `interface Task`, אחרי `source_ref`:

```ts
  board: TaskBoard;
  /** 1..7, dev board only. Labels live in lib/task-boards.ts. */
  phase: number | null;
  /** marketing board only. */
  channel: MktChannel | null;
  /** 0..100, marketing board only. */
  progress: number | null;
```

- [ ] **Step 4: ליצור `types/task-rule.types.ts`**

```ts
/** A recurring-task rule: the rule is data, the generator is code
 *  (lib/services/task-rules/<domain>.ts). Spec §2. */
import type { TaskBoard, TaskPriority } from "@/types/task.types";

export const RULE_DOMAINS = ["price_light", "price_changes", "creative_gaps", "custom"] as const;
export type RuleDomain = (typeof RULE_DOMAINS)[number];

export const RULE_MODES = ["weekly_digest", "per_item"] as const;
export type RuleMode = (typeof RULE_MODES)[number];

/** Every field is optional - a rule filters only on what it sets. */
export interface RuleMatch {
  /** price_light: "package" | "ticket"; omitted = package. */
  scope?: "package" | "ticket";
  /** price_light: "football" | "music" … (event vertical tag). */
  vertical?: string;
  /** price_light: only gaps at least this many USD. */
  min_gap_usd?: number;
  /** price_light: only events red for at least N weeks (needs events.light_red_since). */
  min_weeks_red?: number;
  /** price_changes: only frozen rows deviating at least this much. */
  min_deviation_usd?: number;
  /** price_changes: ignore log rows older than this. */
  max_age_days?: number;
  /** creative_gaps: which gap kinds count. Empty = all. */
  kinds?: string[];
  /** creative_gaps: 0 = every gap, 1 = severe only. */
  min_severity?: number;
}

export interface TaskRule {
  id: string;
  name: string;
  domain: RuleDomain;
  mode: RuleMode;
  match: RuleMatch;
  assignee_id: string | null;
  priority: TaskPriority;
  due_days: number | null;
  /** 0 = Sunday … 6 = Saturday. */
  dow: number;
  board: TaskBoard;
  /** custom domain only - the rule IS the task. */
  title: string | null;
  description: string | null;
  active: boolean;
  last_run_at: string | null;
  created_by: string | null;
  created_at: string;
  updated_at: string;
}

export interface TaskRuleWithNames extends TaskRule {
  assignee_name: string | null;
}
```

- [ ] **Step 5: ליצור `types/task-comment.types.ts`**

```ts
/** One thread per task: human comments and system activity rows share a table
 *  so the story reads in one chronological list. Spec §3. */

export interface TaskAttachment {
  /** Storage path inside the private task-attachments bucket: {task_id}/{uuid}.{ext} */
  path: string;
  name: string;
  mime: string;
  size: number;
  width: number | null;
  height: number | null;
}

export const ACTIVITY_FIELDS = [
  "status", "assignee", "priority", "due_date", "progress", "board",
] as const;
export type ActivityField = (typeof ACTIVITY_FIELDS)[number];

export interface TaskActivity {
  field: ActivityField;
  from: string | null;
  to: string | null;
}

export interface TaskComment {
  id: string;
  task_id: string;
  author_id: string | null;
  kind: "comment" | "activity";
  body: string | null;
  activity: TaskActivity | null;
  attachments: TaskAttachment[];
  mentions: string[];
  edited_at: string | null;
  deleted_at: string | null;
  created_at: string;
}

export interface TaskCommentWithAuthor extends TaskComment {
  author_name: string | null;
  /** Display names of `mentions`, same order. */
  mention_names: string[];
  /** Signed URLs for `attachments`, same order, valid one hour. */
  attachment_urls: string[];
}
```

- [ ] **Step 6: שער הטיפוסים**

Run: `npx tsc --noEmit`
Expected: השגיאות היחידות הן במפות שממפות `TaskStatus` לתווית — `STATUS_LABEL` ב-`tasks-client.tsx` וכל מפה אחרת שחסרה `paused`. לתקן ולהריץ שוב עד שנקי.
תווית `paused`: `"מושהה"` בעברית, `"Paused"` באנגלית.

- [ ] **Step 7: עצור ודווח לדור**

```
feat(tasks): tasks hub schema - boards, rules, thread, light_red_since
```

---

### Task 2: לוחות, פאזות וערוצים בקוד + בטופס

**Files:**
- Create: `lib/task-boards.ts`
- Create: `scripts/task-boards-selftest.ts`
- Modify: `lib/actions/task-actions.ts` (`TASK_COLUMNS`, `createTask`, `updateTask`)
- Modify: `components/task-editor.tsx`

**Interfaces:**
- Consumes: `TaskBoard`, `MktChannel`, `TASK_BOARDS`, `MKT_CHANNELS` (Task 1).
- Produces: `BOARD_META`, `PHASES`, `CHANNEL_META`, `validBoard()`, `validPhase()`, `validChannel()`, `validProgress()`; `createTask`/`updateTask` מקבלים `board`/`phase`/`channel`/`progress`.

- [ ] **Step 1: לכתוב את ה-self-test שנכשל**

```ts
// scripts/task-boards-selftest.ts - `npx tsx scripts/task-boards-selftest.ts`
// Pure: no DB, no network. Same pattern as scripts/price-light-selftest.ts.
import { BOARD_META, CHANNEL_META, PHASES, validBoard, validChannel, validPhase, validProgress } from "../lib/task-boards";

let failed = 0;
function check(name: string, got: unknown, want: unknown) {
  const ok = JSON.stringify(got) === JSON.stringify(want);
  if (!ok) { failed++; console.error(`FAIL ${name}: got ${JSON.stringify(got)} want ${JSON.stringify(want)}`); }
  else console.log(`ok   ${name}`);
}

check("board valid", validBoard("dev"), true);
check("board invalid", validBoard("engineering"), false);
check("phase 1..7", [validPhase(1), validPhase(7), validPhase(0), validPhase(8), validPhase(null)], [true, true, false, false, true]);
check("channel", [validChannel("seo"), validChannel("tiktok")], [true, false]);
check("progress bounds", [validProgress(0), validProgress(100), validProgress(101), validProgress(-1)], [true, true, false, false]);
check("every phase has a label", Object.keys(PHASES).length, 7);
check("every channel has a label", Object.keys(CHANNEL_META).length, 7);
check("every board has a label", Object.keys(BOARD_META).length, 3);

console.log(failed ? `\n${failed} FAILED` : "\nall passed");
process.exit(failed ? 1 : 0);
```

- [ ] **Step 2: להריץ ולראות שהוא נכשל**

Run: `npx tsx scripts/task-boards-selftest.ts`
Expected: FAIL — `Cannot find module '../lib/task-boards'`

- [ ] **Step 3: לכתוב את `lib/task-boards.ts`**

```ts
/** Labels for the three boards, the seven roadmap phases and the marketing
 *  channels. The ONLY place these strings live (spec §1.1) - the old roadmap
 *  app's palette is not carried over; colours come from the MYT Admin tokens. */
import { MKT_CHANNELS, TASK_BOARDS, type MktChannel, type TaskBoard } from "@/types/task.types";

export const BOARD_META: Record<TaskBoard, { label: string; hint: string }> = {
  dev: { label: "פיתוח", hint: "מפת הדרכים של המוצר" },
  marketing: { label: "שיווק", hint: "קמפיינים, תוכן, שותפויות" },
  ops: { label: "תפעול", hint: "מה שהמערכת מייצרת + משימות שוטפות" },
};

export const PHASES: Record<number, { name: string; sub: string }> = {
  1: { name: "ליבה ותפעול", sub: "ספקים, הזמנות, כרטיסים" },
  2: { name: "תשלומים ומסחר", sub: "פיצולים, חיוב, גמישות כספית" },
  3: { name: "עיצוב ו-UX", sub: "מיתוג, חווית משתמש" },
  4: { name: "בק אופיס ואופרציה", sub: "כלים פנימיים, אוטומציה, דוחות" },
  5: { name: "מוצרים מתקדמים", sub: "חבילות קומפלקס, טורנירים" },
  6: { name: "שיווק וצמיחה", sub: "דיוור, אוטומציות, B2B" },
  7: { name: "AI וטכנולוגיה", sub: "בינה מלאכותית, CMS, אינטגרציות" },
};

export const CHANNEL_META: Record<MktChannel, { label: string }> = {
  social: { label: "סושיאל" },
  email: { label: "אימייל" },
  seo: { label: "SEO" },
  ads: { label: "ממומן" },
  content: { label: "תוכן" },
  partnerships: { label: "שותפויות" },
  pr: { label: "PR" },
};

export function validBoard(value: unknown): value is TaskBoard {
  return typeof value === "string" && (TASK_BOARDS as readonly string[]).includes(value);
}

/** null is valid - only the dev board carries a phase. */
export function validPhase(value: unknown): boolean {
  if (value === null || value === undefined) return true;
  return typeof value === "number" && Number.isInteger(value) && value >= 1 && value <= 7;
}

export function validChannel(value: unknown): boolean {
  if (value === null || value === undefined) return true;
  return typeof value === "string" && (MKT_CHANNELS as readonly string[]).includes(value);
}

export function validProgress(value: unknown): boolean {
  if (value === null || value === undefined) return true;
  return typeof value === "number" && Number.isInteger(value) && value >= 0 && value <= 100;
}
```

- [ ] **Step 4: להריץ שוב**

Run: `npx tsx scripts/task-boards-selftest.ts`
Expected: כל השורות `ok`, `all passed`

- [ ] **Step 5: לחבר ל-`task-actions.ts`**

`TASK_COLUMNS` מקבל את ארבע העמודות:

```ts
const TASK_COLUMNS =
  "id,title,description,status,priority,assignee_id,created_by,due_date,source,source_ref," +
  "board,phase,channel,progress,deleted_at,completed_at,created_at,updated_at";
```

`createTask` ו-`updateTask` מקבלים אותן ב-`input`/`patch` וכותבים **עמודה-עמודה** (בלי spread — חוק mass-assignment), עם דחייה מפורשת:

```ts
if (input.board !== undefined && !validBoard(input.board)) return { ok: false, error: "Bad board" };
if (!validPhase(input.phase)) return { ok: false, error: "Bad phase" };
if (!validChannel(input.channel)) return { ok: false, error: "Bad channel" };
if (!validProgress(input.progress)) return { ok: false, error: "Bad progress" };
```

ובאובייקט ה-insert/update: `board: input.board ?? "ops"`, `phase: input.phase ?? null`, `channel: input.channel ?? null`, `progress: input.progress ?? null`.

- [ ] **Step 6: שדות בטופס**

ב-`components/task-editor.tsx`: בורר לוח (3 אפשרויות, ברירת מחדל `ops`); כשהלוח `dev` מופיע בורר פאזה (7 + "ללא"); כשהלוח `marketing` מופיעים בורר ערוץ ומחוון התקדמות 0–100. מעבר לוח מנקה את השדות שאינם שייכים לו — אחרת משימה ששונתה מ-`marketing` ל-`dev` נושאת ערוץ רפאים.

- [ ] **Step 7: אימות**

Run: `npx tsc --noEmit && npm run lint`
Expected: נקי.
בדפדפן (`preview_start` על `dev`): `/tasks` → New task → "שיווק" → לוודא ערוץ והתקדמות → לשמור → לוודא שהשורה נשמרה עם הערכים.

- [ ] **Step 8: עצור ודווח לדור**

```
feat(tasks): boards, phases and marketing channels on tasks
```

---

### Task 3: `events.light_red_since` — מאז מתי אדום

**Files:**
- Modify: `lib/services/price-light-store.ts` (`LIGHT_EVENT_COLUMNS`, `LightEvent`, `recomputeEventLights`)
- Create: `scripts/light-red-since-selftest.ts`

**Interfaces:**
- Consumes: `Lights`, `Light` הקיימים.
- Produces: `redSinceUpdate(before: Lights, after: Lights, current: string | null, now: string): { light_red_since: string | null } | null` — מיוצא מ-`price-light-store.ts`, נצרך ע"י הגנרטור ב-Task 8.

- [ ] **Step 1: לכתוב את ה-self-test שנכשל**

```ts
// scripts/light-red-since-selftest.ts - `npx tsx scripts/light-red-since-selftest.ts`
import { redSinceUpdate } from "../lib/services/price-light-store";

const NOW = "2026-09-16T00:00:00.000Z";
const OLD = "2026-09-01T00:00:00.000Z";
let failed = 0;
function check(name: string, got: unknown, want: unknown) {
  const ok = JSON.stringify(got) === JSON.stringify(want);
  if (!ok) { failed++; console.error(`FAIL ${name}: got ${JSON.stringify(got)} want ${JSON.stringify(want)}`); }
  else console.log(`ok   ${name}`);
}

check("enters red", redSinceUpdate({ package: "orange", ticket: null }, { package: "red", ticket: null }, null, NOW), { light_red_since: NOW });
check("stays red", redSinceUpdate({ package: "red", ticket: null }, { package: "red", ticket: null }, OLD, NOW), null);
check("second scope reds", redSinceUpdate({ package: "red", ticket: "green" }, { package: "red", ticket: "red" }, OLD, NOW), null);
check("leaves red", redSinceUpdate({ package: "red", ticket: null }, { package: "orange", ticket: null }, OLD, NOW), { light_red_since: null });
check("quiet", redSinceUpdate({ package: "green", ticket: "green" }, { package: "green", ticket: "green" }, null, NOW), null);
check("re-reds", redSinceUpdate({ package: "green", ticket: null }, { package: "red", ticket: null }, null, NOW), { light_red_since: NOW });
// Rows that were already red before the column existed get stamped, not left blank.
check("backfills missing stamp", redSinceUpdate({ package: "red", ticket: null }, { package: "red", ticket: null }, null, NOW), { light_red_since: NOW });

console.log(failed ? `\n${failed} FAILED` : "\nall passed");
process.exit(failed ? 1 : 0);
```

- [ ] **Step 2: להריץ ולראות כישלון**

Run: `npx tsx scripts/light-red-since-selftest.ts`
Expected: FAIL — `redSinceUpdate is not a function`

- [ ] **Step 3: לממש**

ב-`lib/services/price-light-store.ts`:

```ts
/** When did this event become red? Written by recomputeEventLights, read by the
 *  price_light rule's `min_weeks_red`. Null return = write nothing. */
export function redSinceUpdate(
  before: Lights,
  after: Lights,
  current: string | null,
  now: string,
): { light_red_since: string | null } | null {
  const wasRed = before.package === "red" || before.ticket === "red";
  const isRed = after.package === "red" || after.ticket === "red";
  if (isRed) {
    // A red with no stamp gets one now - including rows that were red before
    // the column existed. "Red since we started counting" beats "unknown".
    return current ? null : { light_red_since: now };
  }
  if (wasRed || current) return { light_red_since: null };
  return null;
}
```

בתוך `recomputeEventLights`, ליד `clearSilence`:

```ts
const redSince = redSinceUpdate(before, after, event.light_red_since, now);
```

ובאובייקט ה-update: `...(redSince ?? {})`.

`LIGHT_EVENT_COLUMNS` מקבל `light_red_since`, ו-`LightEvent` מקבל:

```ts
  /** When the event last ENTERED red (either scope). Null = not red. */
  light_red_since: string | null;
```

- [ ] **Step 4: להריץ שוב**

Run: `npx tsx scripts/light-red-since-selftest.ts`
Expected: `all passed`

- [ ] **Step 5: אימות בלי כתיבה**

Run: `npx tsc --noEmit`
ואז מול דב מקומי: `curl "http://localhost:3000/api/cron/price-light-nightly?dry_run=1&key=$NEXT_SECRET_CRON_SECRET_KEY"`
Expected: מסתיים בלי שגיאה. `dry_run` לא כותב — העמודה מתמלאת בריצה הלילית האמיתית הראשונה אחרי הדיפלוי.

- [ ] **Step 6: עצור ודווח לדור**

```
feat(price-light): record when an event entered red (light_red_since)
```

---

### Task 4: פתיל השיח — actions + שורות activity

**Files:**
- Create: `lib/actions/task-comment-actions.ts`
- Create: `lib/services/task-activity.ts`
- Create: `scripts/task-thread-selftest.ts`
- Modify: `lib/actions/task-actions.ts` (`updateTask`, `setTaskStatus`)

**Interfaces:**
- Consumes: `TaskComment`, `TaskCommentWithAuthor`, `TaskActivity`, `TaskAttachment` (Task 1); `requireStaff`, `logAudit`.
- Produces:
  - `listTaskComments(taskId: string): Promise<TaskCommentWithAuthor[]>`
  - `addTaskComment(input: { taskId: string; body: string; attachments?: TaskAttachment[]; mentions?: string[] }): Promise<{ ok: true; id: string } | { ok: false; error: string }>`
  - `editTaskComment(id: string, body: string): Promise<Ok>`
  - `deleteTaskComment(id: string): Promise<Ok>`
  - `recordActivity(taskId: string, actorId: string | null, activity: TaskActivity): Promise<void>` (מ-`task-activity.ts`)
  - `diffActivities(before: Record<string, unknown>, after: Record<string, unknown>): TaskActivity[]` (טהור)
  - `type Ok = { ok: true } | { ok: false; error: string }`

- [ ] **Step 1: לכתוב את ה-self-test שנכשל**

```ts
// scripts/task-thread-selftest.ts - `npx tsx scripts/task-thread-selftest.ts`
// Pure helpers only: the DB paths are verified in the browser.
import { diffActivities } from "../lib/services/task-activity";

let failed = 0;
function check(name: string, got: unknown, want: unknown) {
  const ok = JSON.stringify(got) === JSON.stringify(want);
  if (!ok) { failed++; console.error(`FAIL ${name}: got ${JSON.stringify(got)} want ${JSON.stringify(want)}`); }
  else console.log(`ok   ${name}`);
}

check("status change", diffActivities({ status: "todo" }, { status: "in_progress" }),
  [{ field: "status", from: "todo", to: "in_progress" }]);
check("no change", diffActivities({ status: "todo" }, { status: "todo" }), []);
check("assignee cleared", diffActivities({ assignee_id: "u1" }, { assignee_id: null }),
  [{ field: "assignee", from: "u1", to: null }]);
check("two fields", diffActivities({ priority: "low", due_date: null }, { priority: "urgent", due_date: "2026-09-20" }),
  [{ field: "priority", from: "low", to: "urgent" }, { field: "due_date", from: null, to: "2026-09-20" }]);
check("untracked field ignored", diffActivities({ title: "a" }, { title: "b" }), []);
check("number to string", diffActivities({ progress: 10 }, { progress: 60 }),
  [{ field: "progress", from: "10", to: "60" }]);

console.log(failed ? `\n${failed} FAILED` : "\nall passed");
process.exit(failed ? 1 : 0);
```

- [ ] **Step 2: להריץ ולראות כישלון**

Run: `npx tsx scripts/task-thread-selftest.ts`
Expected: FAIL — `Cannot find module '../lib/services/task-activity'`

- [ ] **Step 3: לכתוב את `lib/services/task-activity.ts`**

```ts
/** System rows in the task thread. Written from the same server action that
 *  performs the change - a DB trigger would not know who the actor was. */
import { supabase } from "@/lib/supabase-server";
import type { ActivityField, TaskActivity } from "@/types/task-comment.types";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
const db = supabase as any;

/** Column name in `tasks` → the field name shown in the thread. */
const TRACKED: Record<string, ActivityField> = {
  status: "status",
  assignee_id: "assignee",
  priority: "priority",
  due_date: "due_date",
  progress: "progress",
  board: "board",
};

function asText(value: unknown): string | null {
  if (value === null || value === undefined || value === "") return null;
  return String(value);
}

/** Pure: which tracked fields actually changed. Untracked keys are ignored. */
export function diffActivities(
  before: Record<string, unknown>,
  after: Record<string, unknown>,
): TaskActivity[] {
  const out: TaskActivity[] = [];
  for (const [column, field] of Object.entries(TRACKED)) {
    if (!(column in after)) continue;
    const from = asText(before[column]);
    const to = asText(after[column]);
    if (from === to) continue;
    out.push({ field, from, to });
  }
  return out;
}

/** Best-effort: a failed activity row never fails the change it describes. */
export async function recordActivity(
  taskId: string,
  actorId: string | null,
  activity: TaskActivity,
): Promise<void> {
  const { error } = await db.from("task_comments").insert({
    task_id: taskId,
    author_id: actorId,
    kind: "activity",
    activity,
  });
  if (error) console.error("task-activity: insert failed", JSON.stringify(error));
}
```

- [ ] **Step 4: להריץ שוב**

Run: `npx tsx scripts/task-thread-selftest.ts`
Expected: `all passed`

- [ ] **Step 5: לכתוב את `lib/actions/task-comment-actions.ts`**

```ts
"use server";

import { requireStaff } from "@/lib/auth/guards";
import { supabase } from "@/lib/supabase-server";
import { logAudit } from "@/lib/audit";
import { ADMIN_ROLES } from "@/types/auth.types";
import type { TaskAttachment, TaskComment, TaskCommentWithAuthor } from "@/types/task-comment.types";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
const db = supabase as any;

export type Ok = { ok: true } | { ok: false; error: string };

const COMMENT_COLUMNS =
  "id,task_id,author_id,kind,body,activity,attachments,mentions,edited_at,deleted_at,created_at";

const BODY_MAX = 5000;
const STAFF = ["superadmin", "admin", "editor"];

function isManager(role: string): boolean {
  return (ADMIN_ROLES as readonly string[]).includes(role);
}

/** Every staff member reads every thread (spec §3.4). */
export async function listTaskComments(taskId: string): Promise<TaskCommentWithAuthor[]> {
  await requireStaff();

  const { data, error } = await db
    .from("task_comments")
    .select(COMMENT_COLUMNS)
    .eq("task_id", taskId)
    .order("created_at", { ascending: true });
  if (error) {
    console.error("task-comments: list failed", JSON.stringify(error));
    return [];
  }
  const rows = (data ?? []) as TaskComment[];

  const ids = [...new Set(rows.flatMap((r) => [r.author_id, ...r.mentions]).filter((v): v is string => !!v))];
  const nameOf = new Map<string, string>();
  if (ids.length) {
    const { data: users, error: userError } = await db
      .from("user_profiles").select("id,display_name,email").in("id", ids);
    if (userError) console.error("task-comments: names failed", JSON.stringify(userError));
    for (const user of (users ?? []) as { id: string; display_name: string | null; email: string }[]) {
      nameOf.set(user.id, user.display_name || user.email);
    }
  }

  const urlOf = await signedUrlMap(rows.flatMap((r) => (r.deleted_at ? [] : r.attachments.map((a) => a.path))));

  return rows.map((row) => ({
    ...row,
    // A deleted comment keeps its place in the thread but gives up its content.
    body: row.deleted_at ? null : row.body,
    attachments: row.deleted_at ? [] : row.attachments,
    author_name: row.author_id ? (nameOf.get(row.author_id) ?? null) : null,
    mention_names: row.mentions.map((id) => nameOf.get(id) ?? "משתמש"),
    attachment_urls: row.deleted_at ? [] : row.attachments.map((a) => urlOf.get(a.path) ?? ""),
  }));
}

/** One signed URL per path, valid an hour. Private bucket - never a public URL. */
async function signedUrlMap(paths: string[]): Promise<Map<string, string>> {
  const out = new Map<string, string>();
  if (!paths.length) return out;
  const { data, error } = await supabase.storage.from("task-attachments").createSignedUrls(paths, 3600);
  if (error) {
    console.error("task-comments: sign failed", JSON.stringify(error));
    return out;
  }
  for (const item of data ?? []) {
    if (item.path && item.signedUrl) out.set(item.path, item.signedUrl);
  }
  return out;
}

export async function addTaskComment(input: {
  taskId: string;
  body: string;
  attachments?: TaskAttachment[];
  mentions?: string[];
}): Promise<{ ok: true; id: string } | { ok: false; error: string }> {
  const session = await requireStaff();

  const body = input.body?.trim() ?? "";
  const attachments = input.attachments ?? [];
  if (!body && attachments.length === 0) return { ok: false, error: "אין מה לשלוח" };
  if (body.length > BODY_MAX) return { ok: false, error: "התגובה ארוכה מדי" };

  // Mentions come from the picker, but the client is not trusted with them:
  // keep only ids that are real staff profiles.
  const mentions = await staffIdsOnly(input.mentions ?? []);

  const { data, error } = await db
    .from("task_comments")
    .insert({
      task_id: input.taskId,
      author_id: session.sub,
      kind: "comment",
      body: body || null,
      attachments,
      mentions,
    })
    .select("id")
    .single();
  if (error || !data) {
    console.error("task-comments: add failed", JSON.stringify(error));
    return { ok: false, error: "השליחה נכשלה" };
  }

  await logAudit({ action: "task.comment", entityType: "task", entityId: input.taskId, changes: { comment_id: data.id, mentions } });
  return { ok: true, id: data.id };
}

async function staffIdsOnly(ids: string[]): Promise<string[]> {
  const unique = [...new Set(ids)];
  if (!unique.length) return [];
  const { data, error } = await db.from("user_profiles").select("id,role").in("id", unique);
  if (error) {
    console.error("task-comments: mention check failed", JSON.stringify(error));
    return [];
  }
  return (data ?? [])
    .filter((user: { role: string }) => STAFF.includes(user.role))
    .map((user: { id: string }) => user.id);
}

/** Author edits their own. An admin does NOT edit someone else's words - a
 *  comment edited by another hand is a forged quote. Admin power is deletion. */
export async function editTaskComment(id: string, body: string): Promise<Ok> {
  const session = await requireStaff();
  const text = body?.trim();
  if (!text) return { ok: false, error: "תגובה ריקה" };
  if (text.length > BODY_MAX) return { ok: false, error: "התגובה ארוכה מדי" };

  const { data, error } = await db
    .from("task_comments")
    .update({ body: text, edited_at: new Date().toISOString() })
    .eq("id", id)
    .eq("author_id", session.sub)
    .is("deleted_at", null)
    .select("id");
  if (error) {
    console.error("task-comments: edit failed", JSON.stringify(error));
    return { ok: false, error: "העריכה נכשלה" };
  }
  if (!data?.length) return { ok: false, error: "אפשר לערוך רק תגובה שלך" };
  return { ok: true };
}

/** Soft delete: the row keeps its place, the content goes. */
export async function deleteTaskComment(id: string): Promise<Ok> {
  const session = await requireStaff();

  let query = db
    .from("task_comments")
    .update({ deleted_at: new Date().toISOString() })
    .eq("id", id)
    .eq("kind", "comment");
  if (!isManager(session.role)) query = query.eq("author_id", session.sub);

  const { data, error } = await query.select("id,task_id");
  if (error) {
    console.error("task-comments: delete failed", JSON.stringify(error));
    return { ok: false, error: "המחיקה נכשלה" };
  }
  if (!data?.length) return { ok: false, error: "אין הרשאה למחוק את התגובה" };

  await logAudit({ action: "task.comment_delete", entityType: "task", entityId: data[0].task_id, changes: { comment_id: id } });
  return { ok: true };
}
```

- [ ] **Step 6: לחבר שורות activity ל-`task-actions.ts`**

ב-`updateTask`: השאילתה שכבר קוראת את השורה לפני העדכון מקבלת את כל השדות המנוטרים (`status,assignee_id,priority,due_date,progress,board` — היום רק `assignee_id`), ואחרי עדכון מוצלח:

```ts
for (const activity of diffActivities(beforeRow ?? {}, update)) {
  await recordActivity(id, session.sub, activity);
}
```

ב-`setTaskStatus`: להוסיף `status` ל-`select` שקורא את השורה **לפני** העדכון, ואחרי `logAudit`:

```ts
await recordActivity(id, session.sub, { field: "status", from: previousStatus, to: status });
```

- [ ] **Step 7: אימות**

Run: `npx tsc --noEmit && npm run lint && npx tsx scripts/task-thread-selftest.ts`
Expected: נקי + `all passed`.

- [ ] **Step 8: עצור ודווח לדור**

```
feat(tasks): task thread - comments and activity rows
```

---

### Task 5: תמונות — העלאה מאובטחת + קריאה חתומה

**Files:**
- Create: `lib/images/sniff.ts`
- Create: `scripts/image-sniff-selftest.ts`
- Modify: `lib/actions/task-comment-actions.ts` (הוספת `uploadTaskAttachment`)
- Modify: `lib/actions/task-actions.ts` (`deleteTask` מוחק גם את הקבצים)

**Interfaces:**
- Consumes: `TaskAttachment` (Task 1), `Ok` (Task 4).
- Produces: `sniffImageMime(bytes: Uint8Array): "image/png" | "image/jpeg" | "image/webp" | "image/gif" | null`; `uploadTaskAttachment(taskId: string, form: FormData): Promise<{ ok: true; attachment: TaskAttachment } | { ok: false; error: string }>`.

- [ ] **Step 1: לכתוב את ה-self-test שנכשל**

```ts
// scripts/image-sniff-selftest.ts - `npx tsx scripts/image-sniff-selftest.ts`
import { sniffImageMime } from "../lib/images/sniff";

let failed = 0;
function check(name: string, got: unknown, want: unknown) {
  const ok = got === want;
  if (!ok) { failed++; console.error(`FAIL ${name}: got ${String(got)} want ${String(want)}`); }
  else console.log(`ok   ${name}`);
}
const bytes = (...n: number[]) => Uint8Array.from(n);

check("png", sniffImageMime(bytes(0x89, 0x50, 0x4e, 0x47, 0x0d, 0x0a, 0x1a, 0x0a, 0, 0, 0, 0)), "image/png");
check("jpeg", sniffImageMime(bytes(0xff, 0xd8, 0xff, 0xe0, 0, 0, 0, 0, 0, 0, 0, 0)), "image/jpeg");
check("gif", sniffImageMime(bytes(0x47, 0x49, 0x46, 0x38, 0x39, 0x61, 0, 0, 0, 0, 0, 0)), "image/gif");
check("webp", sniffImageMime(bytes(0x52, 0x49, 0x46, 0x46, 1, 2, 3, 4, 0x57, 0x45, 0x42, 0x50)), "image/webp");
// An HTML page renamed screenshot.png must not pass.
check("html rejected", sniffImageMime(bytes(0x3c, 0x21, 0x44, 0x4f, 0x43, 0x54, 0x59, 0x50, 0x45, 0, 0, 0)), null);
check("svg rejected", sniffImageMime(bytes(0x3c, 0x73, 0x76, 0x67, 0, 0, 0, 0, 0, 0, 0, 0)), null);
check("too short", sniffImageMime(bytes(0x89, 0x50)), null);

console.log(failed ? `\n${failed} FAILED` : "\nall passed");
process.exit(failed ? 1 : 0);
```

- [ ] **Step 2: להריץ ולראות כישלון**

Run: `npx tsx scripts/image-sniff-selftest.ts`
Expected: FAIL — `Cannot find module '../lib/images/sniff'`

- [ ] **Step 3: לממש את הזיהוי**

```ts
/** What the bytes actually are. The browser-declared mime is a claim, not a
 *  fact: an HTML page named screenshot.png arrives as image/png. SVG is
 *  rejected on purpose - it is a script container, not a screenshot. */
export type SniffedImage = "image/png" | "image/jpeg" | "image/webp" | "image/gif";

export function sniffImageMime(bytes: Uint8Array): SniffedImage | null {
  if (bytes.length < 12) return null;
  const at = (i: number) => bytes[i];
  if (at(0) === 0x89 && at(1) === 0x50 && at(2) === 0x4e && at(3) === 0x47 &&
      at(4) === 0x0d && at(5) === 0x0a && at(6) === 0x1a && at(7) === 0x0a) return "image/png";
  if (at(0) === 0xff && at(1) === 0xd8 && at(2) === 0xff) return "image/jpeg";
  if (at(0) === 0x47 && at(1) === 0x49 && at(2) === 0x46 && at(3) === 0x38) return "image/gif";
  if (at(0) === 0x52 && at(1) === 0x49 && at(2) === 0x46 && at(3) === 0x46 &&
      at(8) === 0x57 && at(9) === 0x45 && at(10) === 0x42 && at(11) === 0x50) return "image/webp";
  return null;
}
```

- [ ] **Step 4: להריץ שוב**

Run: `npx tsx scripts/image-sniff-selftest.ts`
Expected: `all passed`

- [ ] **Step 5: פעולת ההעלאה**

ב-`lib/actions/task-comment-actions.ts`:

```ts
import { randomUUID } from "crypto";
import { sniffImageMime } from "@/lib/images/sniff";

const ATTACHMENT_MAX_BYTES = 5 * 1024 * 1024;
const EXT: Record<string, string> = {
  "image/png": "png", "image/jpeg": "jpg", "image/webp": "webp", "image/gif": "gif",
};

/** Upload one pasted/dropped image. The path is derived server-side from the
 *  task id and a uuid - a client-supplied name never reaches the storage path. */
export async function uploadTaskAttachment(
  taskId: string,
  form: FormData,
): Promise<{ ok: true; attachment: TaskAttachment } | { ok: false; error: string }> {
  await requireStaff();

  const file = form.get("file");
  if (!(file instanceof File)) return { ok: false, error: "לא התקבל קובץ" };
  if (file.size > ATTACHMENT_MAX_BYTES) return { ok: false, error: "הקובץ גדול מ-5MB" };

  const buffer = Buffer.from(await file.arrayBuffer());
  const mime = sniffImageMime(new Uint8Array(buffer.subarray(0, 12)));
  if (!mime) return { ok: false, error: "אפשר להעלות תמונות בלבד (PNG/JPG/WebP/GIF)" };

  // The task must exist and not be deleted - otherwise the bucket collects
  // orphan folders no screen will ever show.
  const { data: task, error: taskError } = await db
    .from("tasks").select("id").eq("id", taskId).is("deleted_at", null).maybeSingle();
  if (taskError || !task) return { ok: false, error: "המשימה לא נמצאה" };

  const path = `${taskId}/${randomUUID()}.${EXT[mime]}`;
  const { error } = await supabase.storage
    .from("task-attachments")
    .upload(path, buffer, { contentType: mime, upsert: false });
  if (error) {
    console.error("task-comments: upload failed", JSON.stringify(error));
    return { ok: false, error: "ההעלאה נכשלה" };
  }

  const width = Number(form.get("width"));
  const height = Number(form.get("height"));
  return {
    ok: true,
    attachment: {
      path,
      // Display name only - never used to build a path.
      name: (file.name || "screenshot").slice(0, 120),
      mime,
      size: file.size,
      width: Number.isFinite(width) && width > 0 ? width : null,
      height: Number.isFinite(height) && height > 0 ? height : null,
    },
  };
}
```

- [ ] **Step 6: מחיקת משימה מנקה את הקבצים**

ב-`deleteTask` (`lib/actions/task-actions.ts`), אחרי המחיקה הרכה:

```ts
  // The thread rows survive the soft delete; the images do not need to.
  const { data: files } = await supabase.storage.from("task-attachments").list(id);
  if (files?.length) {
    const { error: removeError } = await supabase.storage
      .from("task-attachments")
      .remove(files.map((file) => `${id}/${file.name}`));
    if (removeError) console.error("tasks: attachment cleanup failed", JSON.stringify(removeError));
  }
```

- [ ] **Step 7: אימות**

Run: `npx tsc --noEmit && npm run lint && npx tsx scripts/image-sniff-selftest.ts`
Expected: נקי + `all passed`. (ההעלאה עצמה נבדקת ב-Task 6 מול ה-UI.)

- [ ] **Step 8: עצור ודווח לדור**

```
feat(tasks): private image attachments with magic-byte validation
```

---

### Task 6: `task-thread.tsx` — הפתיל על המסך

**Files:**
- Create: `components/task-thread.tsx`
- Modify: `components/task-editor.tsx` (להציג את הפתיל בעריכת משימה קיימת)
- Modify: `app/(dashboard)/tasks/tasks-client.tsx` (עמודת 💬)

**Interfaces:**
- Consumes: `listTaskComments`, `addTaskComment`, `editTaskComment`, `deleteTaskComment`, `uploadTaskAttachment` (Tasks 4–5); `listUsers` מ-`lib/actions/user-actions.ts` לבורר האזכורים.
- Produces: `<TaskThread taskId={string} />`.

- [ ] **Step 1: לבנות את הקומפוננטה**

`"use client"`, עם הכותרת:

```tsx
// components/task-thread.tsx
// The conversation on one task: comments, pasted screenshots, @mentions and
// the system's own activity rows, in one chronological list (spec §3).
```

- `useEffect` טוען `listTaskComments(taskId)`, `<Skeleton />` עד שחזר.
- שורה: אווטאר/שם, זמן, גוף. שורת `activity` בטקסט מעומעם דרך מפה מקומית `ACTIVITY_LABEL` (`status` → "שינה סטטוס", `assignee` → "שייך ל-", `priority` → "שינה עדיפות", `due_date` → "שינה יעד", `progress` → "עדכן התקדמות", `board` → "העביר לוח").
- תגובה מחוקה: `"התגובה נמחקה"` מעומעם, בלי תוכן ובלי תמונות.
- תיבת כתיבה: `<Textarea>` + כפתור שליחה; `Ctrl+Enter` שולח.

- [ ] **Step 2: הדבקה, גרירה ובחירת קובץ**

```tsx
async function uploadFiles(files: File[]) {
  for (const file of files.slice(0, 5)) {
    if (!file.type.startsWith("image/")) continue;
    const id = crypto.randomUUID();
    setPending((prev) => [...prev, { id, name: file.name }]);
    const shrunk = await shrinkToMaxWidth(file, 2000); // canvas; returns the original when already narrow
    const form = new FormData();
    form.set("file", shrunk.file);
    form.set("width", String(shrunk.width));
    form.set("height", String(shrunk.height));
    const result = await uploadTaskAttachment(taskId, form);
    setPending((prev) => prev.filter((item) => item.id !== id));
    if (!result.ok) { toast({ title: result.error, variant: "destructive" }); continue; }
    setAttachments((prev) => [...prev, result.attachment]);
  }
}
```

מחובר לשלושה מקורות על אזור הכתיבה: `onPaste` (`Array.from(event.clipboardData.files)`), `onDrop` (`event.preventDefault()` + `Array.from(event.dataTransfer.files)`), ו-`<input type="file" accept="image/*" multiple>` מוסתר מאחורי כפתור מהדק.

`shrinkToMaxWidth` היא פונקציה מקומית בקובץ: ציור ל-`<canvas>`, `canvas.toBlob` → `File` באותו mime.

- [ ] **Step 3: אזכורים**

הקלדת `@` פותחת רשימת staff (נטענת פעם אחת ב-`useEffect`), סינון בהקלדה, בחירה מכניסה `@<display_name> ` לטקסט ומוסיפה uuid ל-`mentions`; המצב מתאפס בשליחה. אין פרסור טקסט בשרת — ה-uuid הוא המקור.

- [ ] **Step 4: לייטבוקס**

לחיצה על thumbnail פותחת `<Dialog>` של shadcn עם `attachment_urls[i]` במלואה; `Esc` סוגר. בלי ספרייה.

- [ ] **Step 5: חיבור לעורך ולטבלה**

ב-`task-editor.tsx`: כשיש `id` (עריכה) — `<TaskThread taskId={id} />` מתחת לטופס. במשימה חדשה אין פתיל.
ב-`tasks-client.tsx`: עמודה עם `MessageSquare` ומספר התגובות, משדה `comment_count` ש-`listTasks` מחזירה. הספירה נטענת בשאילתה **אחת** ומצטברת במפה בצד השרת:

```ts
const { data: counts } = await db
  .from("task_comments")
  .select("task_id")
  .eq("kind", "comment")
  .is("deleted_at", null);
```

- [ ] **Step 6: אימות בדפדפן**

Run: `npx tsc --noEmit && npm run lint`, ואז `preview_start` על `dev`.
ב-`/tasks`: לפתוח משימה → לכתוב תגובה → Ctrl+V עם צילום מסך → thumbnail → לייטבוקס → `@` ובחירת אדם → שליחה → רענון ולוודא שהכל חזר → שינוי סטטוס ולוודא שורת activity → מחיקת תגובה ולוודא "התגובה נמחקה".
`read_console_messages` — אפס שגיאות.

- [ ] **Step 7: עצור ודווח לדור**

```
feat(tasks): task thread UI - paste screenshots, mentions, activity
```

---

### Task 7: מייל אזכור

**Files:**
- Create: `lib/services/task-mention-notify.ts`
- Modify: `lib/actions/task-comment-actions.ts` (קריאה מ-`addTaskComment`)

**Interfaces:**
- Consumes: `sendMail`, `appOrigin` מ-`lib/email.ts`; `mentions` מ-Task 4.
- Produces: `notifyTaskMention(input: { taskId: string; taskTitle: string; body: string; authorId: string | null; mentionIds: string[] }): Promise<void>`.

- [ ] **Step 1: לכתוב את השירות**

```ts
/** "You were mentioned in a task" mail. Sister of task-notify.ts: same
 *  transport, same best-effort contract - a mail failure is logged and never
 *  fails the comment write (spec §3.3). */
import { appOrigin, sendMail } from "@/lib/email";
import { supabase } from "@/lib/supabase-server";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
const db = supabase as any;

const EXCERPT_MAX = 300;

export interface TaskMentionInput {
  taskId: string;
  taskTitle: string;
  body: string;
  authorId: string | null;
  mentionIds: string[];
}

export async function notifyTaskMention(input: TaskMentionInput): Promise<void> {
  try {
    const targets = input.mentionIds.filter((id) => id !== input.authorId);
    if (!targets.length) return;

    const ids = [...targets, ...(input.authorId ? [input.authorId] : [])];
    const { data, error } = await db.from("user_profiles").select("id,email,display_name").in("id", ids);
    if (error) throw error;
    const profiles = (data ?? []) as { id: string; email: string; display_name: string | null }[];
    const author = profiles.find((p) => p.id === input.authorId);
    const authorName = author?.display_name || author?.email || "מישהו";

    const url = `${appOrigin()}/tasks?task=${input.taskId}`;
    const excerpt = input.body.length > EXCERPT_MAX ? `${input.body.slice(0, EXCERPT_MAX)}…` : input.body;

    for (const target of profiles.filter((p) => targets.includes(p.id))) {
      if (!target.email) continue;
      await sendMail({
        to: target.email,
        subject: `אוזכרת במשימה: ${input.taskTitle}`,
        html: `<div dir="rtl" style="font-family:Arial,sans-serif">
  <p>${escapeHtml(authorName)} אזכר/ה אותך במשימה <strong>${escapeHtml(input.taskTitle)}</strong>:</p>
  <blockquote style="border-right:3px solid #5BFF95;margin:0;padding:0 12px;color:#333">${escapeHtml(excerpt)}</blockquote>
  <p><a href="${url}">למשימה</a></p>
</div>`,
        text: [`${authorName} אזכר/ה אותך במשימה: ${input.taskTitle}`, excerpt, url].join("\n"),
      });
    }
  } catch (error) {
    console.error("task-mention-notify failed", error);
  }
}

function escapeHtml(value: string): string {
  return value
    .replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;").replace(/'/g, "&#39;");
}
```

- [ ] **Step 2: לקרוא לו מ-`addTaskComment`**

אחרי `logAudit`, לפני ה-`return`:

```ts
  if (mentions.length) {
    const { data: task } = await db.from("tasks").select("title").eq("id", input.taskId).maybeSingle();
    await notifyTaskMention({
      taskId: input.taskId,
      taskTitle: task?.title ?? "משימה",
      body,
      authorId: session.sub,
      mentionIds: mentions,
    });
  }
```

- [ ] **Step 3: אימות**

Run: `npx tsc --noEmit && npm run lint`
בדפדפן: אזכור עצמי → לא נשלח מייל (הכותב מסונן); אזכור חשבון אחר → לוודא בלוג שהמייל יצא.

- [ ] **Step 4: עצור ודווח לדור**

```
feat(tasks): mail a staff member when a comment mentions them
```

---

### Task 8: מנוע הכללים — גנרטורים

**Files:**
- Create: `lib/services/task-rules/types.ts`
- Create: `lib/services/task-rules/week.ts`
- Create: `lib/services/task-rules/price-light.ts`
- Create: `lib/services/task-rules/price-changes.ts`
- Create: `lib/services/task-rules/creative-gaps.ts`
- Create: `lib/services/task-rules/custom.ts`
- Create: `lib/services/task-rules/index.ts`
- Create: `scripts/task-rules-selftest.ts`

**Interfaces:**
- Consumes: `TaskRule`, `RuleMatch`, `RuleDomain` (Task 1); `light_red_since` (Task 3); `listAllCreativeGaps` מ-`lib/actions/creative-gap-actions.ts`; טבלת `base_price_sync_log`.
- Produces:
  - `interface RuleCandidate { key: string; title: string; description: string; sourceRef: TaskSourceRef }`
  - `interface RuleGenerator { domain: RuleDomain; label: string; screenUrl: string; digestTitle(count: number): string; candidates(match: RuleMatch): Promise<RuleCandidate[]> }`
  - `generatorFor(domain: RuleDomain): RuleGenerator`
  - `isoWeek(date: Date): string`, `weeksSince(iso: string | null, now: Date): number | null`
  - `filterLightCandidates(rows, match, now)` (טהור, מיוצא לבדיקה)

- [ ] **Step 1: לכתוב את ה-self-test שנכשל**

```ts
// scripts/task-rules-selftest.ts - `npx tsx scripts/task-rules-selftest.ts`
// Pure only: week maths and the price-light filter, fed synthetic rows.
import { isoWeek, weeksSince } from "../lib/services/task-rules/week";
import { filterLightCandidates } from "../lib/services/task-rules/price-light";

let failed = 0;
function check(name: string, got: unknown, want: unknown) {
  const ok = JSON.stringify(got) === JSON.stringify(want);
  if (!ok) { failed++; console.error(`FAIL ${name}: got ${JSON.stringify(got)} want ${JSON.stringify(want)}`); }
  else console.log(`ok   ${name}`);
}

check("iso week", isoWeek(new Date("2026-09-16T00:00:00Z")), "2026-W38");
check("iso week rolls at monday", isoWeek(new Date("2026-09-21T00:00:00Z")), "2026-W39");
check("weeks since null", weeksSince(null, new Date("2026-09-16T00:00:00Z")), null);
check("weeks since 15 days", weeksSince("2026-09-01T00:00:00Z", new Date("2026-09-16T00:00:00Z")), 2);

const rows = [
  { id: 1, name: "A", light_package: "red", light_ticket: null, light_red_since: "2026-09-01T00:00:00Z", vertical: "football", gap_usd: 200, silenced: false },
  { id: 2, name: "B", light_package: "red", light_ticket: null, light_red_since: "2026-09-14T00:00:00Z", vertical: "football", gap_usd: 200, silenced: false },
  { id: 3, name: "C", light_package: "orange", light_ticket: null, light_red_since: null, vertical: "football", gap_usd: 900, silenced: false },
  { id: 4, name: "D", light_package: "red", light_ticket: null, light_red_since: "2026-09-01T00:00:00Z", vertical: "music", gap_usd: 200, silenced: false },
  { id: 5, name: "E", light_package: "red", light_ticket: null, light_red_since: "2026-09-01T00:00:00Z", vertical: "football", gap_usd: 20, silenced: false },
  { id: 6, name: "F", light_package: "red", light_ticket: null, light_red_since: "2026-09-01T00:00:00Z", vertical: "football", gap_usd: 200, silenced: true },
  { id: 7, name: "G", light_package: "red", light_ticket: null, light_red_since: null, vertical: "football", gap_usd: 200, silenced: false },
];
const now = new Date("2026-09-16T00:00:00Z");

check("no filter = every unsilenced red", filterLightCandidates(rows, {}, now).map((r) => r.id), [1, 2, 4, 5, 7]);
check("vertical", filterLightCandidates(rows, { vertical: "football" }, now).map((r) => r.id), [1, 2, 5, 7]);
check("min gap", filterLightCandidates(rows, { min_gap_usd: 150 }, now).map((r) => r.id), [1, 2, 4, 7]);
// min_weeks_red drops the 2-day-old red AND the unknown one (null = not proven).
check("min weeks red", filterLightCandidates(rows, { min_weeks_red: 2 }, now).map((r) => r.id), [1, 4, 5]);

console.log(failed ? `\n${failed} FAILED` : "\nall passed");
process.exit(failed ? 1 : 0);
```

- [ ] **Step 2: להריץ ולראות כישלון**

Run: `npx tsx scripts/task-rules-selftest.ts`
Expected: FAIL — `Cannot find module '../lib/services/task-rules/week'`

- [ ] **Step 3: `week.ts`**

```ts
/** ISO week key ("2026-W38") - the dedupe key of a weekly digest, and the unit
 *  min_weeks_red counts in. */
export function isoWeek(date: Date): string {
  const d = new Date(Date.UTC(date.getUTCFullYear(), date.getUTCMonth(), date.getUTCDate()));
  // ISO: a week belongs to the year of its Thursday.
  const day = d.getUTCDay() || 7;
  d.setUTCDate(d.getUTCDate() + 4 - day);
  const yearStart = new Date(Date.UTC(d.getUTCFullYear(), 0, 1));
  const week = Math.ceil(((d.getTime() - yearStart.getTime()) / 86_400_000 + 1) / 7);
  return `${d.getUTCFullYear()}-W${String(week).padStart(2, "0")}`;
}

/** Whole weeks between an ISO timestamp and now. null in = null out: an unknown
 *  start is not zero weeks, and must never satisfy a minimum. */
export function weeksSince(iso: string | null, now: Date): number | null {
  if (!iso) return null;
  const then = new Date(iso).getTime();
  if (!Number.isFinite(then)) return null;
  return Math.floor((now.getTime() - then) / (7 * 86_400_000));
}
```

- [ ] **Step 4: `types.ts` + `price-light.ts`**

```ts
// lib/services/task-rules/types.ts
import type { RuleDomain, RuleMatch } from "@/types/task-rule.types";
import type { TaskSourceRef } from "@/types/task.types";

export interface RuleCandidate {
  /** Stable identity for per_item dedupe: "{kind}:{table}:{row_id}". */
  key: string;
  title: string;
  description: string;
  sourceRef: TaskSourceRef;
}

export interface RuleGenerator {
  domain: RuleDomain;
  /** Hebrew label for the digest title and the rules screen. */
  label: string;
  /** Where the digest task links to. */
  screenUrl: string;
  digestTitle(count: number): string;
  /** READ ONLY. A generator never writes a price and never removes an event. */
  candidates(match: RuleMatch): Promise<RuleCandidate[]>;
}
```

```ts
// lib/services/task-rules/price-light.ts
import { supabase } from "@/lib/supabase-server";
import { weeksSince } from "./week";
import type { RuleGenerator } from "./types";
import type { RuleMatch } from "@/types/task-rule.types";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
const db = supabase as any;

export interface LightRow {
  id: number;
  name: string;
  light_package: string | null;
  light_ticket: string | null;
  light_red_since: string | null;
  vertical: string | null;
  gap_usd: number | null;
  silenced: boolean;
}

/** Pure: which red events this rule wants. Exported for the self-test. */
export function filterLightCandidates(rows: LightRow[], match: RuleMatch, now: Date): LightRow[] {
  return rows.filter((row) => {
    // Scope defaults to "package" - the same default candidates() uses to read
    // the gap and the vertical, so the filter and the numbers never disagree.
    const red = match.scope === "ticket" ? row.light_ticket === "red" : row.light_package === "red";
    if (!red) return false;
    if (row.silenced) return false;
    if (match.vertical && row.vertical !== match.vertical) return false;
    if (match.min_gap_usd != null && (row.gap_usd ?? 0) < match.min_gap_usd) return false;
    if (match.min_weeks_red != null) {
      const weeks = weeksSince(row.light_red_since, now);
      // Unknown start = not proven = no task. A quiet week beats a false wave on
      // the first night after the column ships (spec §1.3).
      if (weeks === null || weeks < match.min_weeks_red) return false;
    }
    return true;
  });
}

export const priceLightGenerator: RuleGenerator = {
  domain: "price_light",
  label: "רמזור",
  screenUrl: "/price-light?f=pending",
  digestTitle: (count) => `סקירת רמזור שבועית — ${count} אדומים ממתינים להחלטה`,
  async candidates(match) {
    const scope = match.scope ?? "package";
    const { data, error } = await db
      .from("events")
      .select("id,name,light_package,light_ticket,light_red_since,light_silenced_until,light_detail")
      .is("is_deleted", null)
      .gte("date", new Date().toISOString().slice(0, 10));
    if (error) {
      console.error("task-rules/price-light: load failed", JSON.stringify(error));
      return [];
    }
    const now = new Date();
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const rows: LightRow[] = (data ?? []).map((row: any) => ({
      id: row.id,
      name: row.name,
      light_package: row.light_package,
      light_ticket: row.light_ticket,
      light_red_since: row.light_red_since,
      vertical: row.light_detail?.[scope]?.vertical ?? null,
      gap_usd: row.light_detail?.[scope]?.diff_usd ?? null,
      silenced: !!row.light_silenced_until && new Date(row.light_silenced_until) > now,
    }));

    return filterLightCandidates(rows, match, now).map((row) => ({
      key: `${scope}:events:${row.id}`,
      title: `רמזור אדום: ${row.name}`,
      description: `פער $${row.gap_usd ?? "?"} מול המתחרים · אדום מאז ${row.light_red_since?.slice(0, 10) ?? "לא ידוע"}`,
      sourceRef: { kind: scope, table: "events", row_id: row.id, label: row.name, url: `/events/${row.id}#fix-price` },
    }));
  },
};
```

- [ ] **Step 5: `price-changes.ts`, `creative-gaps.ts`, `custom.ts`**

`price-changes`: קורא `base_price_sync_log` בסטטוס `needs_review`, מסנן `min_deviation_usd` ו-`max_age_days`, `screenUrl = "/price-changes"`, `digestTitle: (count) => \`שינויי מחיר להחלטה — ${count} רשומות קפואות\``, `sourceRef.kind = "price_review"`, `url: "/events/<id>#fix-price"`.

`creative-gaps`: קורא `listAllCreativeGaps()` הקיים, מסנן `kinds` ו-`min_severity`, `screenUrl = "/tasks?tab=gaps"`, `digestTitle: (count) => \`פערי קריאייטיב — ${count} נכסים חסרים\``, ו-`sourceRef` מועתק מהפער (`kind`,`table`,`row_id`,`label`,`url`) — בדיוק מה שכפתור "Do" בונה היום.

`custom`: `candidates()` מחזירה `[]`; `digestTitle()` מחזירה מחרוזת ריקה (הכותרת מגיעה מ-`rule.title`, שהמנוע מעדיף כשהוא קיים).

- [ ] **Step 6: `index.ts`**

```ts
import type { RuleDomain } from "@/types/task-rule.types";
import type { RuleGenerator } from "./types";
import { priceLightGenerator } from "./price-light";
import { priceChangesGenerator } from "./price-changes";
import { creativeGapsGenerator } from "./creative-gaps";
import { customGenerator } from "./custom";

const GENERATORS: Record<RuleDomain, RuleGenerator> = {
  price_light: priceLightGenerator,
  price_changes: priceChangesGenerator,
  creative_gaps: creativeGapsGenerator,
  custom: customGenerator,
};

export function generatorFor(domain: RuleDomain): RuleGenerator {
  return GENERATORS[domain];
}

export type { RuleCandidate, RuleGenerator } from "./types";
```

- [ ] **Step 7: להריץ ולוודא טיפוסים**

Run: `npx tsx scripts/task-rules-selftest.ts && npx tsc --noEmit && npm run lint`
Expected: `all passed` + נקי.

- [ ] **Step 8: עצור ודווח לדור**

```
feat(tasks): rule generators for price light, price changes and creative gaps
```

---

### Task 9: `weeklyTaskGen` — הריצה השבועית

**Files:**
- Create: `lib/services/weekly-task-gen.ts`
- Create: `app/api/cron/weeklyTaskGen/route.ts`
- Modify: `vercel.json`

**Interfaces:**
- Consumes: `generatorFor`, `isoWeek` (Task 8); `notifyTaskAssigned` מ-`lib/services/task-notify.ts`.
- Produces: `runWeeklyTaskGen(opts: { dryRun?: boolean; budgetMs?: number; ruleId?: string; now?: Date }): Promise<TaskGenSummary>`;
  `interface TaskGenSummary { ran: number; created: number; existed: number; closed: number; skipped: { rule: string; why: string }[]; errors: string[]; dryRun: boolean; wouldCreate: { rule: string; title: string }[] }`.

- [ ] **Step 1: לכתוב את השירות**

```ts
/** The weekly recurring-task run (spec §2.1). One cron for every rule; the rule
 *  is data, the generator is code. Reads only - it never writes a price.
 *  The insert is direct (not createTask): this runs with no session. */
export async function runWeeklyTaskGen(opts: {
  dryRun?: boolean; budgetMs?: number; ruleId?: string; now?: Date;
} = {}): Promise<TaskGenSummary> {
  const now = opts.now ?? new Date();
  const deadline = Date.now() + (opts.budgetMs ?? 270_000);
  const week = isoWeek(now);
  const summary: TaskGenSummary = {
    ran: 0, created: 0, existed: 0, closed: 0, skipped: [], errors: [],
    dryRun: !!opts.dryRun, wouldCreate: [],
  };

  // Oldest visit first, so a budget cutoff never starves the same rule week
  // after week (same rotation idea as base-price-sync).
  let query = db.from("task_rules").select("*").eq("active", true)
    .order("last_run_at", { ascending: true, nullsFirst: true });
  if (opts.ruleId) query = query.eq("id", opts.ruleId);
  const { data, error } = await query;
  if (error) { summary.errors.push(`load rules: ${error.message}`); return summary; }

  for (const rule of (data ?? []) as TaskRule[]) {
    if (Date.now() > deadline) { summary.skipped.push({ rule: rule.name, why: "budget" }); continue; }
    // A manual "run now" ignores the day; the cron respects it.
    if (!opts.ruleId && rule.dow !== now.getUTCDay()) {
      summary.skipped.push({ rule: rule.name, why: "not its day" });
      continue;
    }
    try {
      await runOneRule(rule, week, now, summary, !!opts.dryRun);
      summary.ran++;
    } catch (e) {
      summary.errors.push(`${rule.name}: ${e instanceof Error ? e.message : "failed"}`);
    }
  }
  return summary;
}
```

`runOneRule` עושה בדיוק את זה:

1. `const generator = generatorFor(rule.domain);`
   `const candidates = rule.domain === "custom" ? [] : await generator.candidates(rule.match);`
2. **`weekly_digest`**:
   - `sourceRef = { kind: "rule", table: "task_rules", row_id: rule.id, label: rule.name, url: generator.screenUrl, week }`
   - dedupe: משימה לא מחוקה עם `.contains("source_ref", { row_id: rule.id, week })` → `summary.existed++` ויציאה.
   - `candidates.length === 0` ו-`domain !== "custom"` → `summary.skipped.push({ rule: rule.name, why: "no candidates" })`, **ובנוסף** סגירת דייג'סט פתוח מהשבועות הקודמים: `status: "done"`, הערה `"נסגר אוטומטית — אין יותר פריטים פתוחים"` מתווספת לתיאור (לא דורסת אותו), `summary.closed++`.
   - אחרת יצירה: `title = rule.title || generator.digestTitle(candidates.length)`, `description` = עשר השורות הראשונות מ-`candidates.map(c => "• " + c.title)` ועוד `"ועוד N"` אם יש יותר.
3. **`per_item`**: לכל מועמד, dedupe מול משימה פתוחה עם אותו `{ row_id, kind }` ב-`source_ref` (`existed++`), אחרת insert.
4. כל insert: `source: "recurring"`, `board: rule.board`, `priority: rule.priority`, `assignee_id: rule.assignee_id`, `due_date` = היום + `rule.due_days` (או `null`), `created_by: null` (המערכת), `source_ref` כנ"ל.
5. אחרי insert אמיתי עם `assignee_id` → `notifyTaskAssigned({ …, assignerId: null })` (התבנית כבר מציגה "המערכת" כששולח לא ידוע).
6. `dryRun`: כלום לא נכתב — רק `summary.wouldCreate.push({ rule: rule.name, title })`, ו-`last_run_at` **לא** מתעדכן.
7. בסוף כלל אמיתי: `last_run_at = now.toISOString()` + `logAudit({ action: "tasks.generated", entityType: "task_rule", entityId: rule.id, metadata: { created, existed, closed, candidates: candidates.length } })`.

- [ ] **Step 2: ה-route**

```ts
import { NextRequest, NextResponse } from "next/server";
import { guardCronRoute } from "@/lib/auth/guards";
import { runWeeklyTaskGen } from "@/lib/services/weekly-task-gen";

// Sundays 06:00 UTC (vercel.json). ?dry_run=1 = zero writes.
export const maxDuration = 300;

export async function GET(request: NextRequest) {
  const denied = await guardCronRoute(request);
  if (denied) return denied;
  const dryRun = new URL(request.url).searchParams.get("dry_run") === "1";
  try {
    const summary = await runWeeklyTaskGen({ dryRun, budgetMs: 270_000 });
    console.log(`[weeklyTaskGen] ran=${summary.ran} created=${summary.created} existed=${summary.existed} closed=${summary.closed} errors=${summary.errors.length}${dryRun ? " (dry-run)" : ""}`);
    return NextResponse.json(summary);
  } catch (error) {
    console.error("[weeklyTaskGen] fatal", JSON.stringify(error));
    return NextResponse.json({ error: "weekly task gen failed" }, { status: 500 });
  }
}
```

- [ ] **Step 3: `vercel.json`**

להוסיף למערך `crons`:

```json
    {
      "path": "/api/cron/weeklyTaskGen",
      "schedule": "0 6 * * 0"
    }
```

- [ ] **Step 4: אימות יבש**

Run: `npx tsc --noEmit && npm run lint`
ואז מול דב מקומי: `curl "http://localhost:3000/api/cron/weeklyTaskGen?dry_run=1&key=$NEXT_SECRET_CRON_SECRET_KEY"`
Expected: JSON עם `dryRun: true`, `created: 0`, ורשימת `wouldCreate`. אפס שורות חדשות ב-`tasks` — לוודא בשאילתה.

- [ ] **Step 5: עצור ודווח לדור**

```
feat(tasks): weekly recurring task generator cron
```

---

### Task 10: מסך הכללים `/tasks/rules`

**Files:**
- Create: `lib/actions/task-rule-actions.ts`
- Create: `app/(dashboard)/tasks/rules/page.tsx`
- Create: `app/(dashboard)/tasks/rules/rules-client.tsx`
- Modify: `lib/nav.ts`

**Interfaces:**
- Consumes: `TaskRule`, `RuleMatch`, `RULE_DOMAINS`, `RULE_MODES` (Task 1); `runWeeklyTaskGen` (Task 9); `requireAdmin`.
- Produces: `listTaskRules()`, `createTaskRule(input)`, `updateTaskRule(id, patch)`, `deleteTaskRule(id)`, `runRuleNow(id)`, `previewRule(id)` — כולם `requireAdmin()`.

- [ ] **Step 1: ה-actions**

כל פעולה פותחת ב-`await requireAdmin()`, ממפה עמודות מפורשות (בלי spread), ומאמתת: `domain` ב-`RULE_DOMAINS`, `mode` ב-`RULE_MODES`, `priority` ב-`TASK_PRIORITIES`, `board` ב-`TASK_BOARDS`, `dow` שלם 0–6, `due_days` `null` או 1–60, ו-`match` — רק המפתחות המוכרים מ-`RuleMatch`, מספרים סופיים ולא שליליים; מפתח לא מוכר נזרק בשקט (הוא לא שגיאת משתמש, הוא פשוט לא נשמר).

```ts
export async function runRuleNow(id: string) {
  await requireAdmin();
  const summary = await runWeeklyTaskGen({ ruleId: id });
  await logAudit({ action: "tasks.rule_run", entityType: "task_rule", entityId: id, metadata: { created: summary.created, existed: summary.existed } });
  return summary;
}

export async function previewRule(id: string) {
  await requireAdmin();
  return runWeeklyTaskGen({ ruleId: id, dryRun: true });
}
```

- [ ] **Step 2: המסך**

`components/data-table.tsx` עם העמודות: שם · תחום · מצב · יום · משובץ · עדיפות · יעד · פעיל · פעולות.
דיאלוג יצירה/עריכה עם הבוררים, ושדות `match` **לפי התחום**: רמזור → סקופ/ענף/פער/שבועות; שינויי מחיר → סטייה/גיל; פערים → סוגים/חומרה; custom → כותרת ותיאור.
שני כפתורים בכל שורה: **"תצוגה מקדימה"** (דיאלוג עם `wouldCreate`) ו-**"הרץ עכשיו"** (טוסט `נוצרו N · קיימות M`).

- [ ] **Step 3: ניווט**

ב-`lib/nav.ts`, מתחת ל-Tasks:

```ts
      {
        name: "Task rules",
        href: "/tasks/rules",
        icon: Repeat,
        roles: ADMIN_ROLES,
        keywords: "recurring rules automation כללים משימות אוטומטיות",
      },
```

- [ ] **Step 4: אימות**

Run: `npx tsc --noEmit && npm run lint`
בדפדפן: ליצור כלל `price_light` · `weekly_digest` · ראשון · משויך לעצמך · `min_gap_usd: 150` → "תצוגה מקדימה" (רשימה הגיונית, אפס כתיבות) → "הרץ עכשיו" → משימה אחת ב-`/tasks` → הרצה שנייה מחזירה `existed: 1` בלי כפילות.
כ-editor: `/tasks/rules` חסום והפריט לא בתפריט.

- [ ] **Step 5: עצור ודווח לדור**

```
feat(tasks): task rules screen - create, preview and run recurring rules
```

---

### Task 11: נראות חדשה — כולם רואים, עריכה מוגבלת

**Files:**
- Modify: `lib/actions/task-actions.ts` (`listTasks`, `updateTask`)
- Modify: `app/(dashboard)/tasks/tasks-client.tsx`

**Interfaces:**
- Consumes: `isManager` הקיים.
- Produces: `listTasks()` מחזירה את כל המשימות לכל staff; `updateTask` מרשה ל-editor לעדכן `progress` על משימה שלו בלבד.

- [ ] **Step 1: `listTasks` — בלי סינון לפי משובץ**

למחוק:

```ts
  if (!isManager(session.role)) {
    query = query.eq("assignee_id", session.sub);
  }
```

ולהוסיף במקום:

```ts
  // Everyone on staff sees the whole board (Dor, 16.09): the roadmap lives here
  // now, and a board people cannot see is not a board. Editing stays narrow -
  // an editor only changes the status/progress of tasks assigned to them.
```

- [ ] **Step 2: `updateTask` — לפתוח `progress` ל-editor על משימה שלו**

```ts
  const manager = isManager(session.role);
  const onlyProgress = Object.keys(patch).length === 1 && patch.progress !== undefined;
  if (!manager && !onlyProgress) {
    return { ok: false, error: "רק מנהל עורך פרטי משימה" };
  }
```
וכשלא מנהל — להוסיף `.eq("assignee_id", session.sub)` לשאילתת ה-update, ולהחזיר `{ ok: false, error: "לא המשימה שלך" }` כשלא חזרה שורה.

- [ ] **Step 3: המסך**

ב-`tasks-client.tsx`: מסנן "המשימות שלי" — דלוק כברירת מחדל ל-editor, כבוי לאדמין. מי שרצה רק את שלו לא מרגיש שינוי; הלוח המלא במרחק קליק.

- [ ] **Step 4: אימות**

Run: `npx tsc --noEmit && npm run lint`
בדפדפן כ-editor: לראות משימות של אחרים; לנסות לשנות סטטוס של משימה של אחר → "לא המשימה שלך"; לשנות של עצמו → עובר; להגיב על משימה של אחר → עובר.

- [ ] **Step 5: עצור ודווח לדור**

```
feat(tasks): the whole board is visible to staff, editing stays scoped
```

---

### Task 12: קאנבן + עדשת הלוחות

**Files:**
- Create: `app/(dashboard)/tasks/kanban-board.tsx`
- Modify: `app/(dashboard)/tasks/tasks-client.tsx`

**Interfaces:**
- Consumes: `TaskWithNames`, `setTaskStatus`, `BOARD_META`, `PHASES` (Tasks 1–2).
- Produces: `<KanbanBoard tasks={TaskWithNames[]} onStatusChange={(id: string, status: TaskStatus) => Promise<boolean>} groupBy={"none" | "phase" | "assignee"} />` — ה-`boolean` הוא הצלחה, וממנו הלוח יודע אם להחזיר כרטיס למקומו.

- [ ] **Step 1: הלוח**

`"use client"`. עמודה לכל סטטוס (`todo | in_progress | paused | done | cancelled`; `cancelled` מקופלת). גרירה נייטיב:

```tsx
<article
  draggable={canDrag}
  onDragStart={(e) => e.dataTransfer.setData("text/task-id", task.id)}
  className="rounded-lg border bg-card p-3 shadow-sm"
>
```

```tsx
<section
  onDragOver={(e) => e.preventDefault()}
  onDrop={(e) => {
    const id = e.dataTransfer.getData("text/task-id");
    if (id) void move(id, status);
  }}
>
```

`move` מעדכן אופטימית, קורא ל-`onStatusChange`, ובכישלון מחזיר את הכרטיס למקומו + טוסט עם השגיאה.

- [ ] **Step 2: מובייל**

```tsx
// HTML5 drag does not fire on touch. Below md the card shows a status select
// instead of being draggable - same action, no library.
const [canDrag, setCanDrag] = useState(false);
useEffect(() => {
  const query = window.matchMedia("(min-width: 768px)");
  const apply = () => setCanDrag(query.matches);
  apply();
  query.addEventListener("change", apply);
  return () => query.removeEventListener("change", apply);
}, []);
```

- [ ] **Step 3: עדשה ולשונית**

ב-`tasks-client.tsx`: מעל ה-Tabs, קבוצת כפתורים `הכל / פיתוח / שיווק / תפעול` שכותבת ל-`?board=` וקוראת ממנו (`useSearchParams`), ומסננת גם את הטבלה וגם את הקאנבן. לשונית שלישית `Kanban` מרנדרת את `<KanbanBoard>` עם אותו מערך מסונן, ובורר קיבוץ `ללא / פאזה / משובץ`.

- [ ] **Step 4: אימות בדפדפן**

Run: `npx tsc --noEmit && npm run lint`, ואז `preview_start`:
לגרור כרטיס בין עמודות ולוודא שהסטטוס שורד רענון; להחליף עדשה ולוודא שהטבלה והקאנבן מסתננים יחד ושה-URL משתנה; `resize_window` ל-`mobile` ולוודא בורר סטטוס במקום גרירה; `read_console_messages` נקי.

- [ ] **Step 5: עצור ודווח לדור**

```
feat(tasks): kanban view and board lens
```

---

### Task 13: ייבוא הרודמפ

**Files:**
- Create: `lib/roadmap-import.ts`
- Create: `scripts/import-roadmap.ts`
- Create: `scripts/roadmap-import-selftest.ts`
- Create: `docs/superpowers/roadmap-export-snippet.md`

**Interfaces:**
- Consumes: `TaskBoard`, `MktChannel`, `TaskStatus`, `TaskPriority`, `TaskSourceRef` (Task 1).
- Produces: `mapDevTask(row, userIdByKey)`, `mapMktTask(row, userIdByKey)`, `type RoadmapInsert`.

- [ ] **Step 1: לכתוב את ה-self-test שנכשל**

```ts
// scripts/roadmap-import-selftest.ts - `npx tsx scripts/roadmap-import-selftest.ts`
import { mapDevTask, mapMktTask } from "../lib/roadmap-import";

let failed = 0;
function check(name: string, got: unknown, want: unknown) {
  const ok = JSON.stringify(got) === JSON.stringify(want);
  if (!ok) { failed++; console.error(`FAIL ${name}: got ${JSON.stringify(got)} want ${JSON.stringify(want)}`); }
  else console.log(`ok   ${name}`);
}

const users = new Map([["dor", "u-dor"], ["tom", "u-tom"]]);

check("dev task", mapDevTask({ id: 7, title: "Flights", ph: 1, pri: "critical", st: "inprogress", as: "dor", desc: "d" }, users), {
  title: "Flights", description: "d", status: "in_progress", priority: "urgent",
  assignee_id: "u-dor", board: "dev", phase: 1, channel: null, progress: null,
  source: "roadmap",
  source_ref: { kind: "roadmap_dev", table: "roadmap", row_id: 7, label: "Flights", url: "/tasks" },
});

check("mkt task", mapMktTask({ id: "m3", title: "Newsletter", ch: "email", pri: "medium", st: "paused", as: "tom", prog: 40, desc: "" }, users), {
  title: "Newsletter", description: null, status: "paused", priority: "medium",
  assignee_id: "u-tom", board: "marketing", phase: null, channel: "email", progress: 40,
  source: "roadmap",
  source_ref: { kind: "roadmap_mkt", table: "roadmap", row_id: "m3", label: "Newsletter", url: "/tasks" },
});

check("planning maps to todo", mapMktTask({ id: "m4", title: "X", ch: "seo", pri: "low", st: "planning", as: null, prog: 0, desc: "" }, users).status, "todo");
check("active maps to in_progress", mapMktTask({ id: "m5", title: "Y", ch: "ads", pri: "low", st: "active", as: null, prog: 0, desc: "" }, users).status, "in_progress");
check("unassigned stays null", mapDevTask({ id: 8, title: "Z", ph: 3, pri: "low", st: "todo", as: null, desc: "" }, users).assignee_id, null);

console.log(failed ? `\n${failed} FAILED` : "\nall passed");
process.exit(failed ? 1 : 0);
```

- [ ] **Step 2: להריץ ולראות כישלון**

Run: `npx tsx scripts/roadmap-import-selftest.ts`
Expected: FAIL — `Cannot find module '../lib/roadmap-import'`

- [ ] **Step 3: המיפוי**

```ts
/** Pure mapping from the old roadmap app's shapes to task rows (spec §4.2).
 *  Kept out of the script so it can be self-tested without a DB. */
import type { MktChannel, TaskBoard, TaskPriority, TaskSourceRef, TaskStatus } from "@/types/task.types";

export interface RoadmapDevRow { id: number; title: string; ph: number; pri: string; st: string; as: string | null; desc: string }
export interface RoadmapMktRow { id: string; title: string; ch: string; pri: string; st: string; as: string | null; prog: number; desc: string }

export interface RoadmapInsert {
  title: string;
  description: string | null;
  status: TaskStatus;
  priority: TaskPriority;
  assignee_id: string | null;
  board: TaskBoard;
  phase: number | null;
  channel: MktChannel | null;
  progress: number | null;
  source: "roadmap";
  source_ref: TaskSourceRef;
}

const PRIORITY: Record<string, TaskPriority> = {
  critical: "urgent", high: "high", medium: "medium", low: "low",
};

const STATUS: Record<string, TaskStatus> = {
  todo: "todo", inprogress: "in_progress", done: "done",
  planning: "todo", active: "in_progress", paused: "paused",
};

export function mapDevTask(row: RoadmapDevRow, userIdByKey: Map<string, string>): RoadmapInsert {
  return {
    title: row.title,
    description: row.desc?.trim() || null,
    status: STATUS[row.st] ?? "todo",
    priority: PRIORITY[row.pri] ?? "medium",
    assignee_id: row.as ? (userIdByKey.get(row.as) ?? null) : null,
    board: "dev",
    phase: row.ph >= 1 && row.ph <= 7 ? row.ph : null,
    channel: null,
    progress: null,
    source: "roadmap",
    source_ref: { kind: "roadmap_dev", table: "roadmap", row_id: row.id, label: row.title, url: "/tasks" },
  };
}

export function mapMktTask(row: RoadmapMktRow, userIdByKey: Map<string, string>): RoadmapInsert {
  return {
    title: row.title,
    description: row.desc?.trim() || null,
    status: STATUS[row.st] ?? "todo",
    priority: PRIORITY[row.pri] ?? "medium",
    assignee_id: row.as ? (userIdByKey.get(row.as) ?? null) : null,
    board: "marketing",
    phase: null,
    channel: (row.ch as MktChannel) ?? null,
    progress: Number.isFinite(row.prog) ? Math.max(0, Math.min(100, Math.round(row.prog))) : null,
    source: "roadmap",
    source_ref: { kind: "roadmap_mkt", table: "roadmap", row_id: row.id, label: row.title, url: "/tasks" },
  };
}
```

- [ ] **Step 4: להריץ שוב**

Run: `npx tsx scripts/roadmap-import-selftest.ts`
Expected: `all passed`

- [ ] **Step 5: הסקריפט**

`scripts/import-roadmap.ts`, מופעל `npx tsx scripts/import-roadmap.ts <path.json> [--dry-run]`:

1. קורא JSON בצורה `{ tasks: RoadmapDevRow[], mkt: RoadmapMktRow[] }`.
2. בונה `userIdByKey` ממיפוי מפתח→מייל (`dor`, `alon`, `tom`; המיילים נשאלים מדור לפני ההרצה) מול `user_profiles`. **מפתח שמופיע בנתונים ואין לו התאמה → `console.error` + `process.exit(1)`, לפני כל כתיבה.**
3. לכל שורה: לחפש משימה קיימת `source = 'roadmap'` עם `source_ref` שמכיל `{ kind, row_id }` → קיימת: `update` לאותם שדות; אחרת `insert`.
4. `--dry-run`: מדפיס `create N / update M` וחמש כותרות ראשונות מכל קבוצה, בלי לכתוב.
5. סיכום בסוף.

- [ ] **Step 6: snippet הייצוא**

`docs/superpowers/roadmap-export-snippet.md`:

````markdown
# ייצוא הרודמפ מהדפדפן

לפתוח את אפליקציית הרודמפ, F12 → Console, להדביק ולשמור את הפלט כ-`roadmap.json`:

```js
copy(JSON.stringify({
  tasks: JSON.parse(localStorage.getItem('me_tasks') || '{"tasks":[]}').tasks || [],
  mkt: JSON.parse(localStorage.getItem('me_mkt') || '{"tasks":[]}').tasks || [],
}, null, 2))
```

אם מפתח ה-localStorage של השיווק שונה — `Object.keys(localStorage)` ולעדכן את השם בפקודה.
````

- [ ] **Step 7: ריצה יבשה ואז אמיתית**

Run: `npx tsx scripts/import-roadmap.ts roadmap.json --dry-run`
Expected: מספרים הגיוניים (סדר גודל 40+ פיתוח, עשרות שיווק), אפס שגיאות, אפס כתיבות.
אחרי אישור דור על המספרים: `npx tsx scripts/import-roadmap.ts roadmap.json`, ואז הרצה שנייה — חייבת להראות `update` בלבד ו-`create 0` (idempotent).

- [ ] **Step 8: עצור ודווח לדור**

```
feat(tasks): import the roadmap board into tasks
```

---

### Task 14: באנר באפליקציה הישנה + תיעוד

**Files:**
- Modify: `C:\Users\doraz\OneDrive\Desktop\Work\MegaEvent\GitHub\RoadMap\app\page.tsx` (ריפו נפרד — קומיט נפרד)
- Modify: `CLAUDE.md`
- Modify: `app/(dashboard)/guide/guide-content.ts`

**Interfaces:**
- Consumes: —
- Produces: תיעוד מעודכן; באנר באפליקציה הישנה.

- [ ] **Step 1: באנר**

בראש `app/page.tsx` של RoadMap:

```tsx
<div style={{ background: '#0A1A14', color: '#5BFF95', padding: '10px 16px', textAlign: 'center', fontFamily: 'Assistant, sans-serif' }}>
  הלוח עבר ל-<a href="https://<backoffice-domain>/tasks" style={{ color: '#5BFF95', textDecoration: 'underline' }}>בק אופיס → Tasks</a>. המסך הזה נשאר לקריאה בלבד.
</div>
```

- [ ] **Step 2: `CLAUDE.md`**

להוסיף: `task_rules` ו-`task_comments` לרשימת הטבלאות ה-backoffice-only; `weeklyTaskGen` לרשימת הקרונים (ראשון 06:00 UTC, מייצר סקירה שבועית/משימה-לפריט לפי `task_rules`, `?dry_run=1`); `events.light_red_since` לתיאור הרמזור; ופסקה שהרודמפ חי עכשיו ב-`/tasks` עם הלוחות `dev/marketing/ops` והקאנבן.

- [ ] **Step 3: `/guide`**

פרק דו-לשוני חדש: איך נפתחת משימה, איך עובד הפתיל (הדבקת צילום מסך, `@`), מה זה כלל חוזר ומי משנה אותו, ואיך קוראים את הקאנבן. **חוק הריפו: מסך שמשתנה — ה-guide מתעדכן באותו PR.**

- [ ] **Step 4: אימות**

Run: `npx tsc --noEmit && npm run lint`, ובדפדפן ש-`/guide` מציג את הפרק בשתי השפות.

- [ ] **Step 5: עצור ודווח לדור**

```
docs(tasks): document the tasks hub in CLAUDE.md and the guide
```

ובריפו הרודמפ, קומיט נפרד:

```
chore: point the roadmap board at the backoffice
```

---

## אחרי כל הטסקים

- `npm run db:types` — רק **אחרי** שהמיגרציה הוחלה ממאסטר. אז אפשר להוריד את `supabase as any` מהקבצים החדשים (`task-comment-actions.ts`, `task-rule-actions.ts`, `weekly-task-gen.ts`, `task-activity.ts`).
- להזין את כללי הפתיחה שדור יגדיר (מי לוקח רמזור ספורט, רמזור מוזיקה, שינויי מחיר, פערי קריאייטיב) דרך `/tasks/rules` — לא במיגרציה, כדי שהשיוך יישאר דבר שמשנים במסך.
- לוודא בריצה הלילית הראשונה אחרי הדיפלוי ש-`events.light_red_since` מתמלא; רק שבועיים אחריה להפעיל כלל עם `min_weeks_red`.

---

### Task 15: לשונית Pricing ב-`/tasks` + "משימה שנסגרה מורידה את הפער"

דור (16.09): "להוסיף Price טאב למשימות Pricing + רמזור, כלומר שאדע לקחת את הפערים שיש ברמזור
וב-pricing, ואופציה כמו ב-creative task שנוכל להכין משימה - לעשות אותה או לסמן כנעשה", ובהמשך:
"אחרי שמשימה נעשתה - צריך להוריד אותה מה-gaps של כל אחד באשר הוא".

הכרעות דור: **"טופל" = רושם "הוזל" ומסתיר עד החישוב הבא** (לא השתקה לזמן קצוב ולא דחייה
סופית - אם הפער עדיין שם בבדיקה הלילית הבאה השורה חוזרת, וזה נכון: המחיר לא באמת השתנה).
**כל הצוות רואה את הלשונית**, בניגוד ל-`/price-light` שהוא אדמין בלבד.

**Files:**
- Create: `lib/actions/pricing-gap-actions.ts`
- Create: `app/(dashboard)/tasks/pricing-gaps-tab.tsx`
- Create: `lib/services/gap-resolution.ts`
- Create: `scripts/gap-resolution-selftest.ts`
- Modify: `app/(dashboard)/tasks/tasks-client.tsx` (לשונית רביעית)
- Modify: `lib/actions/task-actions.ts` (`setTaskStatus` - ניתוב גנרי במקום ענף creative_gap)

**Interfaces:**
- Consumes: `generatorFor`, `RuleCandidate` (Task 8); `openPriceLightTask` (`lib/services/price-light-tasks.ts`); `markRepriced` (`lib/actions/price-light-actions.ts`); `dismissCreativeGap` / `restoreCreativeGap` / `gapKey`; `openTaskGapKeys`.
- Produces:
  - `listPricingGaps(): Promise<PricingGapRow[]>` — שורה = `{ key, source: "price_light" | "price_changes", scope, eventId, eventName, gapUsd, since, fixUrl, openTaskId }`
  - `openPricingGapTask(key: string): Promise<{ ok: true; taskId: string; existed: boolean } | { ok: false; error: string }>`
  - `markPricingGapHandled(key: string): Promise<Ok>`
  - `resolveGapForTask(task: { id: string; source: TaskSource; source_ref: TaskSourceRef | null }, status: TaskStatus): Promise<void>` (מ-`gap-resolution.ts`)
  - `gapSourceOf(source: TaskSource): GapFamily | null` ו-`gapAction(status: TaskStatus): "close" | "reopen"` (טהורים)

- [ ] **Step 1: לכתוב את ה-self-test שנכשל**

```ts
// scripts/gap-resolution-selftest.ts - `npx tsx scripts/gap-resolution-selftest.ts`
// Pure routing only: which gap family a task belongs to, and whether a status
// change closes or reopens its gap. The DB writes are verified in the browser.
import { gapAction, gapSourceOf } from "../lib/services/gap-resolution";

let failed = 0;
function check(name: string, got: unknown, want: unknown) {
  const ok = JSON.stringify(got) === JSON.stringify(want);
  if (!ok) { failed++; console.error(`FAIL ${name}: got ${JSON.stringify(got)} want ${JSON.stringify(want)}`); }
  else console.log(`ok   ${name}`);
}

check("creative gap task", gapSourceOf("creative_gap"), "creative");
check("price light task", gapSourceOf("price_light"), "pricing");
check("price review task", gapSourceOf("price_review"), "pricing");
check("manual task has no gap", gapSourceOf("manual"), null);
check("roadmap task has no gap", gapSourceOf("roadmap"), null);
// A recurring task carries its domain in source_ref.kind, so it routes by ref, not by source.
check("recurring has no family by source alone", gapSourceOf("recurring"), null);

check("done closes", gapAction("done"), "close");
check("cancelled closes", gapAction("cancelled"), "close");
check("todo reopens", gapAction("todo"), "reopen");
check("in_progress reopens", gapAction("in_progress"), "reopen");
check("paused reopens", gapAction("paused"), "reopen");

console.log(failed ? `\n${failed} FAILED` : "\nall passed");
process.exit(failed ? 1 : 0);
```

- [ ] **Step 2: להריץ ולראות כישלון**

Run: `npx tsx scripts/gap-resolution-selftest.ts`
Expected: FAIL — `Cannot find module '../lib/services/gap-resolution'`

- [ ] **Step 3: `lib/services/gap-resolution.ts` — כלל אחד לכל משפחות הפערים**

היום `setTaskStatus` יודע לסגור פער קריאייטיב בלבד, בתוך `if (row.source === "creative_gap")`.
הקובץ החדש מחליף את הענף בניתוב גנרי, כדי שאותו חוק יחול על כל פער - קיים ועתידי:

```ts
/** A task born from a gap closes that gap when it is done, and reopens it when
 *  it comes back to life. One rule for every gap family (Dor, 16.09: "אחרי
 *  שמשימה נעשתה - צריך להוריד אותה מה-gaps של כל אחד באשר הוא"). Pure routing
 *  here; each family's write lives in its own action. */
import type { TaskSource, TaskStatus } from "@/types/task.types";

export type GapFamily = "creative" | "pricing";

/** Which gap list this task belongs to, by its source alone. `recurring`
 *  returns null on purpose - a recurring task's family is in source_ref.kind. */
export function gapSourceOf(source: TaskSource): GapFamily | null {
  if (source === "creative_gap") return "creative";
  if (source === "price_light" || source === "price_review") return "pricing";
  return null;
}

/** A finished task closes its gap; anything still open puts it back. */
export function gapAction(status: TaskStatus): "close" | "reopen" {
  return status === "done" || status === "cancelled" ? "close" : "reopen";
}
```

`resolveGapForTask` (אותו קובץ, async) עושה את הכתיבות:
- `creative` → בסגירה `dismissCreativeGap({ ...ref, note: "נסגר במשימה" })`, בפתיחה מחדש
  `restoreCreativeGap(gapKey(ref.kind, ref.table, ref.row_id))` — בדיוק ההתנהגות של היום, מועברת.
- `pricing` + `price_light` → בסגירה `markRepriced(eventId, scope)` (אודיט `price_light.repriced`)
  ותו לא: האור מחושב מחדש בלילה, ואם הפער שרד אותו השורה חוזרת. בפתיחה מחדש אין מה לבטל.
- `pricing` + `price_review` → בסגירה מעדכן את שורת `base_price_sync_log` של אותו אירוע
  מ-`needs_review` ל-`reviewed`; בפתיחה מחדש מחזיר ל-`needs_review`. לעמודה אין CHECK
  (`20260902083705_base_price_sync_log.sql`), אז אין שינוי סכמה - רק לעדכן את הערת הערכים.
- `recurring` → ניתוב לפי `source_ref.kind`: `"package"`/`"ticket"` כמו `price_light`,
  `"price_review"` כמו `price_review`, ו-kind של פער קריאייטיב כמו `creative`.
- `manual` / `roadmap` / לא מוכר → no-op.

- [ ] **Step 4: להריץ שוב ולחבר ל-`setTaskStatus`**

Run: `npx tsx scripts/gap-resolution-selftest.ts` → `all passed`
ב-`lib/actions/task-actions.ts`, במקום ענף ה-`creative_gap`:

```ts
  // Every gap family, not just creative: a done task takes its gap off every
  // list that shows it, and reopening puts it back.
  await resolveGapForTask({ id, source: row.source, source_ref: row.source_ref }, status);
```
`resolveGapForTask` בולעת שגיאות ומלווה אותן ב-`console.error` — סגירת פער לעולם לא מפילה את
שינוי הסטטוס עצמו.

- [ ] **Step 5: `listPricingGaps` — מקור אחד לאמת**

`lib/actions/pricing-gap-actions.ts`, `"use server"`, `requireStaff()` (כל הצוות - הכרעת דור):

```ts
const light = await generatorFor("price_light").candidates({});
const changes = await generatorFor("price_changes").candidates({});
```

אותם גנרטורים שהכלל השבועי מריץ (Task 8), כך ש"מה נחשב פער תמחור פתוח" מוגדר במקום אחד ולא
נפרד בין המסך לקרון. כל מועמד הופך לשורה עם `key = ${sourceRef.kind}:${sourceRef.row_id}`;
`openTaskId` מגיע מ-`openTaskGapKeys("price_light")` + `openTaskGapKeys("price_review")`.
`markPricingGapHandled` מפרק את ה-key ומנתב ל-`markRepriced` (רמזור) או לעדכון
`base_price_sync_log` ל-`reviewed` (שינוי מחיר), ומחזיר `Ok`.

- [ ] **Step 6: הלשונית**

`app/(dashboard)/tasks/pricing-gaps-tab.tsx`, ולשונית רביעית ב-`tasks-client.tsx`:
**Tasks · Kanban · Creative gaps · Pricing**. הטבלה היא `components/data-table.tsx`:
אירוע · מקור (תג "רמזור" / "שינוי מחיר") · סקופ · פער ב-$ · מאז · מצב משימה.
שלושה כפתורים בכל שורה, במקביל ללשונית הפערים:
- **משימה** → `openPricingGapTask`; שורה שכבר יש לה משימה פתוחה מציגה קישור אליה במקום כפתור.
- **לתקן** → `<Link href={fixUrl}>` אל `/events/{id}#fix-price`.
- **טופל** → `markPricingGapHandled`, עם טוסט "נרשם. אם הפער יישאר בבדיקה הלילית הוא יחזור".
מסננים: מקור, סקופ, סף פער, ו-"רק בלי משימה" (דלוק כברירת מחדל).

- [ ] **Step 7: אימות**

Run: `npx tsc --noEmit && npm run lint && npx tsx scripts/gap-resolution-selftest.ts`
בדיקה ידנית (דור, אחרי פריסה): לפתוח משימה משורת רמזור ולוודא שהשורה מציגה "יש משימה";
לסמן את המשימה כ-done ולוודא שהיא יורדת גם מלשונית Pricing וגם מ-"ממתינים להחלטה" ב-`/price-light`;
אותו דבר על פער קריאייטיב — שיורד מלשונית הפערים; ולהחזיר משימה ל-todo ולוודא ששתיהן חוזרות.

- [ ] **Step 8: עצור ודווח לדור**

```
feat(tasks): pricing gaps tab and one gap-closing rule for every task source
```

---

### Task 16: פערי קריאייטיב — בלוב מספיק כהירו, קטגוריות-תאומות מפנות לקבוצה/אמן

מקור: לשונית "באגים / תיקונים - אלון תום ודור" במסמך Google Docs של הצוות (16.09):
- "שיש בלוב בעמוד אמן הוא לא מחשיב את זה כתמונת אמן ולכן מעלה את זה כמשימה."
- "בקבוצות כדורגל - אם יש בלוב עם לוגו - האם חובה שגם יהיה תמונת קבוצה?" → תשובה במסמך: "התמונה זה בלוב - אין יש בלוב לא מחייב הירו".
- "עמוד קטגוריה שנפתח בגלל שיש קטגוריה ברצלונה … אני לא אמור לשים כאן בכלל קריאטיב או שזה תקלה?"
- "אם סימנתי בטסק DONE שינקה את זה גם מהקריאטיב גאפ".

מה נבדק במיין (`myt-main`): קטגוריה שההורה שלה `teams`/`artists` ויש לה קבוצה/אמן תואם (אותו `name_english` או `name`)
מרונדרת ב-`app/c/[...slug]/page.tsx` כ-`TeamCmsPage` / `ArtistCmsPage`. שניהם מעבירים `artImageUrl` (הבלוב) להירו,
ולכן בלוב מספיק לראש העמוד. `TeamCmsPage` מקבל את `page_content` של הקטגוריה; `ArtistCmsPage` לא.
תמונת הקטגוריה (`categories.image_url`) מוצגת רק ככרטיס בעמוד המרכז, לא בעמוד התאום עצמו.

הכרעות דור (16.09): **בלוב = אין פער הירו** (מהמסמך). **קטגוריה-תאומה: הפער נשאר ומפנה לקבוצה/אמן.**

**Files:**
- Modify: `lib/services/creative-gaps.ts`
- Create: `lib/services/category-twins.ts` (זיהוי תאומים — מראה של `cmsTwin.ts` במיין)
- Create: `scripts/category-twins-selftest.ts`
- Modify: פעולת השמירה של קבוצה/אמן (מאתרים ב-`lib/actions/template-crud.ts` או בפעולה הייעודית)
- Create: `scripts/backfill-done-gap-dismissals.ts`

- [ ] **Step 1: בלוב מספיק.** בשאילתות `team_hero` ו-`artist_hero`: שורה עם `art_image_url` לא-ריק **איננה פער**
  (לא `demoted` — פשוט לא מופיעה). אם `demoted` נשאר בשימוש רק בשביל זה, להסיר אותו ואת הדירוג שלו ולעדכן את ההערות.
  `team_logo`, `team_gallery`, `artist_gallery`, `*_bio` לא משתנים.

- [ ] **Step 2: זיהוי תאומים.** `lib/services/category-twins.ts` — פונקציה טהורה
  `findTwin(category, hubs, teams, artists): { kind: "team" | "artist"; id: number; name: string } | null`
  שמשכפלת את כלל ההתאמה של המיין: ההורה הוא ה-hub עם `slug` = `teams` / `artists`; התאמה אם
  `lower(trim(category.name_english))` שווה ל-`name_english` או ל-`name` של הקבוצה/אמן, או `lower(trim(category.name))`
  שווה ל-`name` שלהם (לאמת את שמות העמודות מול `football_teams` / `artists` בבקאופיס — במיין השדות נקראים
  `nameDBenglish` / `name`). + self-test: התאמה באנגלית, בעברית, רישיות ורווחים, הורה שגוי → null, בלי התאמה → null.

- [ ] **Step 3: פערי קטגוריה-תאומה.**
  - `category_content`: קטגוריה-תאומה של **אמן** — לא פער (הטקסט לא מוצג). של **קבוצה** — נשאר פער.
  - `category_image`: קטגוריה-תאומה נשארת פער, אבל `label` = `"<שם> (כרטיס עמוד הקבוצה/האמן)"`,
    `detail` = "התמונה מופיעה בכרטיס בעמוד הקבוצות/האמנים. העלאת תמונה לקבוצה ממלאת אותה.",
    ו-`fixUrl` מפנה לעורך הקבוצה/האמן (עוגן התמונה שלו), לא לעורך הקטגוריה.
  - הטעינה: שאילתה אחת ל-hubs, אחת לקבוצות ואחת לאמנים (שמות בלבד) — לא שאילתה לכל קטגוריה; מכבד `strict`.

- [ ] **Step 4: העלאה לקבוצה ממלאת את הקטגוריה.** בשמירה של קבוצה/אמן: אם נשמרה תמונה (הירו, ובהיעדרו בלוב) ויש קטגוריה-תאומה
  עם `image_url` ריק — לעדכן את `image_url` של הקטגוריה לאותה כתובת (עדכון עמודה אחת, `logAudit`
  `category.image_from_twin`). לעולם לא לדרוס תמונה קיימת של קטגוריה. כשל כאן נרשם ולא מפיל את שמירת הקבוצה.
  **השפעה חוצת-פרויקטים:** המיין קורא `categories.image_url` לכרטיסים — זה רק ממלא ערך ריק, אין שינוי סכמה.

- [ ] **Step 5: ניקוי חד-פעמי.** `scripts/backfill-done-gap-dismissals.ts`: מוצא משימות `source = 'creative_gap'` בסטטוס
  `done` שהפער שלהן (`gapKey(kind, table, row_id)`) לא ב-`creative_gap_dismissals`, ומוסיף dismissal עם note
  "נסגר במשימה (השלמה)". `--dry-run` כברירת מחדל — מדפיס מה ייכתב; כתיבה רק עם `--apply`. לא לגעת במשימות.

- [ ] **Step 6: אימות.** `npx tsx scripts/category-twins-selftest.ts`, `npx tsx scripts/task-rules-selftest.ts`,
  `npx tsc --noEmit`, הרצה לקריאה בלבד של `computeOpenCreativeGaps({ strict: true })` לפני/אחרי עם ספירה לכל סוג
  (מצופה: `team_hero`/`artist_hero` יורדים; `category_content` יורד בכמות קטגוריות-האמן), ו-`--dry-run` של הניקוי.

- [ ] **Step 7: עצור ודווח לדור**

```
fix(tasks): blob art satisfies the page hero; twin categories point at their team or artist
```
